# AMANZINE — تدقيق احترافي كامل (Professional Audit)

> نطاق: كامل المشروع بعد تحوّله إلى منصّة أعمال موحّدة. مبني على فحص الكود الفعلي.
> التصنيف: 🔴 حرج · 🟠 عالٍ · 🟡 متوسط · 🟢 منخفض · ✅ سليم.

---

## 1. ملخّص تنفيذي
AMANZINE أصبح منصّة على **محرّكات وعقود ثابتة** (Business/Search/Ranking/Graph/Recommendation/Map/Event Bus/Activity/Analytics/Rules/Notification/Status/AI/Payment/Wallet). المعمار سليم وقابل للتوسّع (Facade + Adapters + Event backbone). **صالح لإطلاق Beta** بعد معالجة بندين تشغيليين (durability + توزيع الأحداث) وبنود البنية التحتية (Redis/Sentry/backup).

**التقدير العام:** أساس قوي (A-). الفجوات ليست معمارية بل **تشغيلية/بنية تحتية**.

---

## 2. المعمار — ✅ قوي
- **Facade لا Store:** الجداول القائمة تبقى مصدر الحقيقة؛ المحرّكات طبقات فوقها. إضافة نوع نشاط = Adapter فقط.
- **Capabilities تقود الواجهة** لا `type` — مرونة عالية.
- **Event Bus مركزي:** Analytics/Rules/Notification/Recommendation مشتركون — لا منطق مبعثر.
- **عقود ثابتة** (businessAPI/Map/Payment) → الهجرة لجدول `businesses` لاحقاً بلا كسر الواجهة.

**ملاحظة معمارية 🟠:** Event Bus و Activity/Analytics **في الذاكرة داخل العملية**:
1. **Durability:** تُفقد عند إعادة تشغيل Railway (الـFeed/التحليلات تبدأ فارغة). 
2. **Scale:** لا تعمل عبر أكثر من نسخة خادم (المشتركون محليون).
**التوصية:** عند تجاوز نسخة واحدة أو الحاجة لثبات → Redis pub/sub + جدول `activities`/`analytics_daily`. العقود جاهزة لهذا التبديل.

---

## 3. الأمن
| # | الشدّة | البند | الحالة |
|---|---|---|---|
| A1 | ✅ | مصادقة كوكيز HttpOnly + تدوير refresh | مُصلَح |
| A2 | ✅ | إخفاء أسرار الطرف الثالث عن العميل (H-1) | مُصلَح |
| A3 | ✅ | عزل المستأجرين + منع IDOR/PII (C-1/C-2/H-6) | مُصلَح |
| A4 | ✅ | توقيع Webhook + Prototype Pollution (H-3/H-2) | مُصلَح |
| A5 | ✅ | **ثغرة المحفظة**: منع المستخدم من إضافة رصيد لنفسه | مُصلَح هذه الجولة |
| A6 | ✅ | الطبقة العامة (Discover/Business) تكشف المعتمَد/المنشور فقط، محدودة المعدّل | سليم |
| A7 | 🟡 | `/api/track` عام: `businessId` من العميل → تضخيم مقاييس (تحليلات فقط، لا مال). محدود المعدّل 300/5د | مقبول؛ يُشدَّد بربط الحدث بجلسة/توقيع لاحقاً |
| A8 | 🟡 | بوابات الدفع (CMI/Stripe) مقاعد — يجب تدقيق التوقيع/الـwebhook عند تفعيلها | مؤجّل لحين الاعتمادات |
| A9 | 🟢 | حصص AI موجودة؛ يُنصح بسقف إنفاق مالي أيضاً | تحسين |

**خلاصة الأمن:** لا ثغرات حرجة مفتوحة. البنود المتبقية تشغيلية أو مرتبطة بتفعيل بوابات.

---

## 4. الأداء
- ✅ فهارس PostgreSQL على المسارات الحارّة (products status/views/category + كل user_id).
- ✅ الخريطة (Leaflet) lazy · بحث بـ debounce · `allSettled` يمنع سقوط النتائج.
- 🟠 **لا تخزين مؤقّت (cache):** Discover/Search يضربان القاعدة لكل طلب. **التوصية:** Redis cache بمفتاح (q+city+filters) TTL 60ث.
- 🟡 `discoverStores` يستعمل subquery لعدّ المنتجات لكل متجر — قد يبطؤ مع نموّ الجداول؛ يُحسَّن بعمود محسوب أو materialized view.
- 🟡 استعلامات Recommendation/Graph تُطلق عدّة `search` متوازية — مقبول الآن، cache يخفّفه.

---

## 5. جودة الكود / الدين التقني
- ✅ المحرّكات الجديدة نقيّة ومُختبَرة (23/23).
- 🟡 **دين قديم مفتوح** (من التدقيق الأصلي): `any` كثيف (M-1) · `ProductsPage.tsx` عملاق ~2300 سطر (M-2) · i18n واجهة غير مربوطة بالكامل (M-3) · بيانات TikTok وهمية (M-7).
- 🟡 حزمة الواجهة كبيرة (chunks >500kB) — يُنصح بمزيد من code-splitting.
- 🟢 رسائل commit أصبحت منضبطة (Conventional Commits).

---

## 6. تغطية الاختبارات
- ✅ **خادم:** 23 اختبار وحدة (secrets, validate, engines: ranking/graph/status/search/ai/analytics/activity/bus).
- ❌ **واجهة:** لا اختبارات مكوّنات.
- ❌ **E2E:** غائبة (Playwright مثبّت — سيناريوهات دخول/بحث/حجز/دفع مقترحة).
- ❌ **تكامل مسارات:** لا supertest لمسارات العزل/الدفع.
**التوصية:** أضف supertest لمسارات الأمن الحرجة + Playwright E2E للتدفّقات الأساسية قبل الإطلاق الواسع.

---

## 7. الجاهزية للإنتاج
| المحور | الحالة |
|--------|--------|
| مصادقة/أمان | ✅ جاهز |
| قاعدة بيانات + فهارس | ✅ جاهز |
| Health check + rate-limit | ✅ |
| Logging | 🟡 morgan + logger — يُنصح JSON structured (pino) في الإنتاج |
| Monitoring/Sentry | ❌ يحتاج DSN |
| Cache (Redis) | ❌ يحتاج نسخة Redis |
| نسخ احتياطي | 🟡 يحتاج pg_dump→S3 (بدل JSON محلي) |
| Durability الأحداث | 🟠 في الذاكرة — يحتاج جدول/Redis |
| بوابات الدفع | 🟡 COD/محفظة/تحويل جاهزة؛ CMI/Stripe مقاعد تحتاج اعتمادات |
| قنوات الإشعار | 🟡 in-app جاهز؛ WhatsApp/Email/Push مقاعد تحتاج تهيئة |
| SEO/SSR | 🔴 SPA بلا SSR → اكتشاف Google ضعيف لصفحات الأنشطة |
| PWA | 🟡 sw.js موجود — يحتاج manifest كامل + offline |

---

## 8. سجلّ المخاطر (Risk Register)
1. **🟠 فقدان التحليلات/الـFeed عند إعادة التشغيل** (in-memory) → أثر: بيانات لوحة التاجر تُصفَّر. تخفيف: جدول activities.
2. **🟠 توزيع الأحداث على نسخة واحدة** → أثر: لا يتوسّع أفقياً. تخفيف: Redis pub/sub.
3. **🔴 SEO** → أثر: ضعف الاكتشاف العضوي (حرج لمنصّة تعتمد على الاكتشاف). تخفيف: SSR/prerender + sitemap.
4. **🟡 لا cache** → أثر: حِمل قاعدة تحت الضغط. تخفيف: Redis.
5. **🟡 بوابات دفع غير مفعّلة** → أثر: لا دفع إلكتروني فعلي بعد. تخفيف: اعتمادات CMI + تدقيق webhook.

---

## 9. التوصيات المرتّبة (قبل الإطلاق الواسع)
1. **Durability:** جدول `activities` + `analytics_daily` (استبدال الذاكرة خلف نفس واجهة Activity/Analytics).
2. **SEO/SSR** لصفحات `/business/:id` و`/explore` + `sitemap.xml` + Open Graph.
3. **Redis:** cache للبحث + rate-limit موزّع + (اختياري) event bus موزّع.
4. **Monitoring + structured logging** (Sentry DSN + pino).
5. **اختبارات:** supertest لمسارات الأمن/الدفع + Playwright E2E.
6. **نسخ احتياطي حقيقي** (pg_dump → S3/Supabase).
7. **تفعيل الدفع** (CMI أولاً — سوق مغربي) **وقنوات الإشعار**.
8. **معالجة الدين القديم** (تقسيم ProductsPage، أنواع Settings، ربط i18n).

---

## 10. الخلاصة
البنية **جاهزة معماريًا** لتكون منصّة مرجعية. لا ثغرات أمنية حرجة مفتوحة. الفجوات المتبقية **تشغيلية وبنية تحتية** (durability, SEO, cache, monitoring, تفعيل الدفع) — كلّها **تُضاف خلف عقود ثابتة دون إعادة تصميم**. الأولوية القصوى قبل إطلاق واسع: **SEO/SSR + durability الأحداث + تفعيل الدفع المغربي**.
