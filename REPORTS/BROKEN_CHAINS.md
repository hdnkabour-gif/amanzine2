# BROKEN_CHAINS — السلاسلُ المقطوعة

> كلُّ سلسلةٍ تبدأ ولا تنتهي. مرتَّبةٌ بالشدّة.
>
> **الشدّة** تُقاس بأثرها على إنسانٍ حقيقيّ لا بصعوبة الإصلاح:
> `CRITICAL` = وعدٌ يُخلَف صامتًا · `HIGH` = قدرةٌ مفقودة · `MEDIUM` = قيمةٌ مهدورة · `LOW` = نظافة.
>
> **الثقة**: `HIGH` = شوهد وقتَ التشغيل · `MEDIUM` = سلسلةُ نداءٍ متتبَّعة · `UNKNOWN` = غيرُ مُثبت.
>
> ~~نطاقُ هذا التدقيق **توثيقيّ**: لا شيءَ هنا أُصلح.~~ **لم يعد كذلك:** طلب
> المالكُ إتمامَ كلّ ما تبقّى، فأُغلقت السلاسلُ المفتوحةُ كلُّها. يبقى ③
> وحدَه — تهيئةُ بيئةٍ لا كود.

---

## ملخّص

> **تحديث 2026-08-02:** ①②⑦ **أُصلحت** — التفاصيل في كلّ بند. ③ تهيئةُ بيئةٍ لا كود.
>
> **تحديث ثانٍ (من لقطات المالك):** ⑫⑬ أُصلحتا، ⑭ وُضّحت. مصدرُها كلُّها لقطاتُ شاشةٍ لصفحاتٍ حقيقيّة — لا مسحٌ آليّ.
>
> **تحديث ثالث:** ⑮⑯⑰ أُصلحت — من لقطات `/notifications` و`/import` و`/editor` و`/delivery`.
>
> **تحديث رابع:** ⑲⑳㉑ — من لقطات `/profile` و`/settings`. حارسُ «مفاتيح التخزين اليتيمة» كشف ⑲⑳ ولم تكشفهما اللقطات.
>
> **تحديث خامس — إغلاقُ ما تبقّى:** ④⑤⑥⑧⑨⑩⑪ أُغلقت كلُّها. تفاصيلُ ما
> فُعل في نهاية كلّ بند. **المتبقّي: ③ وحدَه — تهيئةُ بيئةٍ بيد المالك.**

| # | السلسلة | الشدّة | الثقة | الحالة |
|---|---|---|---|---|
| ① | مدنُ Livo — تحليلُ الردّ خاطئ | **CRITICAL** | HIGH | ✅ **أُصلح** |
| ② | النسخُ الاحتياطيّة تُكتب على قرصٍ زائل | **CRITICAL** | HIGH | ✅ **صار يقول الحقيقة** |
| ③ | وسائطُ التاجر تُفقد عند كلّ نشر | **CRITICAL** | HIGH | ⚙️ تهيئةُ بيئة |
| ④ | الاستيضاح: الجوابُ لا يُعاد تحليلُه | HIGH | HIGH | ✅ **أُصلح** |
| ⑤ | الدفع الإلكترونيّ: ثلاثةُ مقاعدَ فارغة | HIGH | HIGH | ✅ **صار صادقًا** |
| ⑥ | الاكتشافُ والخريطةُ بلا بابٍ في الواجهة | HIGH | HIGH | ✅ **أُصلح** |
| ⑦ | `push` يقول «وصل» بلا مشترِك | MEDIUM | HIGH | ✅ **أُصلح** |
| ⑧ | الولاء: خادمٌ كاملٌ بلا شاشة | MEDIUM | HIGH | ✅ **أُصلح** |
| ⑨ | «أنشطتي»: المخطّطُ يسمح والواجهةُ لا تعرض | MEDIUM | HIGH | ✅ **أُصلح** |
| ⑩ | القدرات: `CapabilityBar` بصفر استيراد | LOW | HIGH | ✅ **رُكِّب** |
| ⑪ | خمسةُ مكوّناتٍ ميّتة | LOW | HIGH | ✅ **حُسم** |
| ⑫ | الربحُ يُحسَب من مصدرين ويبتلع رسمَ التوصيل | **CRITICAL** | HIGH | ✅ **أُصلح** |
| ⑬ | «لا توجد طلبات بعد» فوق ١٦ طلبًا | HIGH | HIGH | ✅ **أُصلح** |
| ⑭ | «حجز» كلمةٌ لنظامين لا صلةَ بينهما | MEDIUM | HIGH | ⚠️ وُضّح، لم يُدمَج |
| ⑮ | سجلُّ التدقيق كاملٌ على الخادم ولا تقرؤه شاشتان | **HIGH** | HIGH | ✅ **أُصلح** |
| ⑯ | «هل الذكاءُ متاح؟» يُجاب في ٤ مواضعَ بـ٣ أجوبة | **HIGH** | HIGH | ✅ **أُصلح** |
| ⑰ | محرّرُ الصور يُسقط اللوغو من التصدير | MEDIUM | HIGH | ✅ **أُصلح** |
| ⑱ | «لا يوجد رابط» فوق شركةٍ موصولة | LOW | HIGH | ✅ **أُصلح** |
| ⑲ | استوديو البانر يقرأ توكنًا لا وجودَ له ⇒ ٤٠١ صامت | **HIGH** | HIGH | ✅ **أُصلح** |
| ⑳ | محرّرُ الصور يستقبل منتجًا لا يُرسله أحد | MEDIUM | HIGH | ✅ **أُصلح** |
| ㉑ | «الثقة ٨٢٪» تحت درعٍ — وهي اكتمالُ ملفٍّ ذاتيّ | MEDIUM | HIGH | ✅ **أُصلح** |

---

## ① مدنُ Livo — تحليلُ الردّ خاطئ

```
التاجر يضغط «اختبار»
        ↓
POST /api/delivery/verify/:id            ✅
        ↓
registry.resolve(row) → livo.provider    ✅
        ↓
livo.provider.getCities(cfg)             ✅ يتّصل ويُستجاب له
        ↓
(result.data || []).map(...)             ❌ ينفجر
```

- **أين ينكسر:** `server/services/delivery/providers/livo.provider.js` → `getCities()`
- **الشدّة:** `CRITICAL`
- **الثقة:** `HIGH` — **شوهد وقتَ التشغيل على واجهة Livo الحقيقيّة**

**السبب — شكلُ الردّ متداخل:**

```json
{ "success": true, "data": { "data": [ … ٤٤١ مدينة … ] } }
```

الكودُ يقرأ `result.data` متوقّعًا مصفوفة، فيجد **كائنًا**. `|| []` لا تحمي: الكائنُ قيمةٌ صادقة، فتُستدعى `.map` عليه وترمي. النتيجةُ الفعليّة المرصودة:

```
❌ قراءة بيانات من الشركة — (result.data || []).map is not a function
```

**الأثر:** **٤٤١ مدينةً لم يستطع AMANZINE قراءتَها قطّ.** ولا يُصلحه مفتاحٌ صحيح — النقطةُ عامّةٌ أصلًا (جُرّبت بمفتاحٍ مزيّف فردّت البيانات كاملة). كلُّ شحنةٍ إلى Livo ترسل اسمَ المدينة نصًّا بدل مُعرِّفها.

**ملاحظةٌ للسياق:** أُصلحت **السباكةُ** حول هذه الدالّة سابقًا (زرُّ الاختبار · استدلالُ المزوّد · حفظُ المدن المجلوبة · الواجهة) وكلُّها تعمل ومُثبتة. لكنّ **التحليلَ نفسَه** لم يُجرَّب قطّ على واجهةٍ حقيقيّة، فبقي العطبُ الأخيرُ مخفيًّا خلف سباكةٍ سليمة. هذا بالضبط ما يكشفه تشغيلُ التطبيق ولا يكشفه أيُّ اختبارِ وحدة.

**✅ أُصلح (2026-08-02).** لا برقعةً في مزوّدٍ واحد، بل في **العقد**:
`contract.js` صار يُصدّر `pickArray()` و`pickObject()` — تفحصان ولا تفترضان،
وتُجرّبان الأشكالَ الشائعة (`data.data` · `data` · `items` · `results`…).
Livo تستعملهما في المدن **والشحنة والتتبّع** (الثلاثةُ كانت تفترض الشكلَ نفسَه).

**مُتحقَّقٌ على الواجهة الحقيقيّة:** `success: true · ٤٤١ مدينة`.

**وحارسٌ يمنع عودةَ الصنف:** اختبارٌ يمسح كلَّ ملفّات `providers/` ويُسقط أيَّ
`(result… || []).map` أو `result.data.x` مباشرة. جُرّب بحقن العطب الأصليّ فسقط.

---

## ② النسخُ الاحتياطيّة تُكتب على قرصٍ زائل

```
setInterval(24h) → doBackup()
        ↓
fs.writeFileSync(server/data/backups/backup-<user>-<date>.json)   ✅ يُكتب
        ↓
إعادةُ نشرٍ على Railway → القرصُ يُمحى                              ❌
```

- **أين ينكسر:** `server/index.js` → `startDailyBackup()`
- **الشدّة:** `CRITICAL` · **الثقة:** `HIGH`

**السبب:** حاوياتُ Railway بلا قرصٍ دائمٍ ما لم يُركَّب مجلّدٌ صريح. ونفسُ الملفّ يحذف ما زاد عن سبعِ نسخ — فيبدو النظامُ ذا سياسةِ استبقاء، وهو لا يستبقي شيئًا عبر النشر.

**الأثر:** «نسخٌ احتياطيّةٌ يوميّة» وعدٌ لا يُوفى. والأخطر: `GET /api/settings/backups` يعرضها للتاجر ⇒ **ثقةٌ في بيانٍ غيرِ موجود**.

**✅ أُصلح (2026-08-02) — بالصدق لا بالادّعاء.** الكتابةُ كانت تنجح؛ **الدوام**
هو ما لم يكن. `server/lib/backup.js` صار يفصل بينهما:

- يكتب في `BACKUP_DIR` أو `RAILWAY_VOLUME_MOUNT_PATH/backups` إن وُجدا ⇒ **دائم**
- وإلّا يكتب في قرص الحاوية ويقول صراحةً إنّها **مؤقّتة** — في السجلّ، وفي
  `GET /api/settings/backups` (`durable:false` + `warning`)، وفي بوّابة الجاهزيّة

فالتاجرُ لا يبني قرارًا على نسخةٍ لا وجودَ لها.

**وبوّابةُ الجاهزيّة صُحّحت:** كانت تقول «خارجَ التطبيق» وهو **خطأ** — النظامُ
داخله. صارت تفحص الدوامَ وتُجيب بنعم أو لا. واختبارُها الذي كان يفرض `null`
عُدِّل: البندُ يخرج من قائمة «غير القابل للفحص» حين يصير مثبَتًا.

---

## ③ وسائطُ التاجر تُفقد عند كلّ نشر

```
POST /api/media/upload → لا CLOUDINARY_* → القرص المحلّيّ → إعادةُ النشر تمحوه
```

- **أين ينكسر:** `server/routes/media.js` (`CLOUDINARY_CONFIGURED` خطأ)
- **الشدّة:** `CRITICAL` · **الثقة:** `HIGH` (السجلّ يُعلنها عند الإقلاع)
- **السبب:** تهيئةٌ ناقصةٌ لا خطأٌ برمجيّ. يُعلَن في `[Media] ⚠️` وفي بوّابة الجاهزيّة.
- **الأثر:** كلُّ صورةِ منتجٍ رفعها تاجرٌ تختفي عند النشر التالي. المنتجُ يبقى، وصورتُه تصير رابطًا ميّتًا.

---

## ④ الاستيضاح: الجوابُ لا يُعاد تحليلُه

```
مُدخلٌ غيرُ مفهوم → clarify()                       ✅
        ↓ سؤالٌ بهويّةٍ ثابتة                        ✅
المستخدمُ يجيب → pickOption()                       ✅ يُسجَّل القياس
        ↓
applyAnswer(signals, id, optionId) → إعادةُ التحليل  ❌ لا تُستدعى
        ↓
تأكيدٌ («فهمت أنّك… صحيح؟») ثمّ تنفيذ                ❌
```

- **أين ينكسر:** `src/pages/LivingHome.tsx` → `pickOption()` يذهب للوجهة مباشرةً
- **الشدّة:** `HIGH` · **الثقة:** `HIGH`
- **السبب:** ميزةٌ منفَّذةٌ جزئيًّا. `applyAnswer` و`escalate` **مبنيّتان ومُختبَرتان** في `src/lib/clarify.ts` ولا تُستدعيان من أيّ شاشة.
- **الأثر:** استيضاحٌ واحدٌ فقط عمليًّا. لا سؤالَ ثانٍ مبنيٌّ على الجواب الأوّل، ولا انتقالَ لإنسانٍ بعد استيضاحين (ADR-0006 غيرُ مفعَّل في الواجهة).


**✅ أُصلح.** الإشاراتُ (`Signals`) ترافق الحوارَ كلَّه في `LivingHome`،
و`applyAnswer` تُدمج الجوابَ فيُبنى السؤالُ الثاني على الأوّل، وبعد
`MAX_CLARIFICATIONS` يُسلَّم لإنسانٍ عبر `NeedCapture` — الفشلُ يصير طلبًا
مؤكّدًا لا شاشةَ اعتذار. `signalsFrom` و`clarificationStep` مُصدَّرتان من
`needEngine` فالاشتقاقُ في موضعٍ واحد. يحرسها `test/brain/clarifyLoop.test.ts`
(١١ اختبارًا) التي تُحاكي ما تفعله الشاشةُ بالضبط.

> **وممّا كشفه تشغيلُ الحلقة على جملٍ حقيقيّة:** «بغيت سبّاك **فالدار
> البيضاء**» كانت تُقرأ طلبَ **عقار** — قاعدةُ العقار تبحث عن «دار» كمقطعٍ
> فتجدها داخل اسم أكبر مدينةٍ في المغرب. ذِكرُ مكانك كان يهدم فهمَ حاجتك.
> والمدينةُ التي يكتبها المستخدمُ كانت تُهمَل تمامًا. `MOROCCAN_CITIES`
> قائمةٌ واحدة، تُنتزَع المدينةُ أوّلًا ثمّ يُقرأ الموضوعُ ممّا بقي.
---

## ⑤ الدفع الإلكترونيّ: ثلاثةُ مقاعدَ فارغة

```
charge() → cod ✅ · transfer ✅ · wallet ✅ · cmi ❌ · stripe ❌ · paypal ❌
```

- **أين ينكسر:** `server/lib/engines/payment.js` → `ADAPTERS.cmi/stripe/paypal`
- **الشدّة:** `HIGH` · **الثقة:** `HIGH` (`TODO` صريحٌ في الكود)
- **السبب:** مقاعدُ معلَنة. `cmi` فيها `TODO: توليد نموذج CMI الموقّع`.
- **الأثر:** لا دفعَ ببطاقةٍ مغربيّة ⇒ لا عمولةَ لكلّ بيع (يطابق تحليل `MARKETPLACE_ANALYSIS.md`). النافذةُ **تُعلن نفسَها** (`status:'unavailable'`) فلا وعدَ كاذبًا للمستخدم.


**✅ صار صادقًا — والمقاعدُ ما زالت مقاعد.** المحوّلاتُ لا تُكتَب بلا عقدِ
تاجرٍ مع البنك/المزوّد، لكنّ **عطبًا حقيقيًّا** كان مختبئًا: المقاعدُ تُرجع
`unavailable` حين ينقص المفتاح، فإذا **ضبط التاجرُ المفتاح** رجعت `pending`
⇒ يُسجَّل دفعٌ في القاعدة ويُذاع `payment.pending` ولم يُرسَل شيءٌ لأيّ
بوّابة. **التهيئةُ وحدَها كانت تُشغّل كذبة.**

القاعدةُ الآن: مقعدٌ غيرُ مُنفَّذٍ يبقى غيرَ متاحٍ مهما اكتملت البيئة
(`implemented: false` لا يُبطله متغيّرُ بيئة)، والبوّابةُ تُغلَق في `charge()`
قبل أن يُنشَأ أيُّ سجلّ. و`methods()` صارت تفصل «لا كود» عن «لا مفتاح» بدل
أن تخلطهما. وأُضيف ما **يمكن** إتمامُه بلا عقد: `transfer` يُرجع بياناتِ
الحساب البنكيّ من الإعدادات، ويقول صراحةً حين لا تكون مضبوطةً — تحويلٌ بلا
رقمِ حسابٍ ليس طريقةَ دفعٍ بل طريقٌ مسدود. (`server/test/payment.test.js` — ٥)
---

## ⑥ الاكتشافُ والخريطةُ بلا بابٍ في الواجهة

```
GET /api/search?view=map → map.build() → {markers,viewport}  ✅
        ↓
Explore.tsx → MapView.tsx (Leaflet)                          ✅ مبنيّ
        ↓
رابطٌ في قائمةٍ أو صفحةٍ رئيسيّة                                ❌
```

- **أين ينكسر:** `src/pages/NavBar.tsx` — لا مدخلَ لـ`/explore` ولا لـ`/feed`
- **الشدّة:** `HIGH` · **الثقة:** `HIGH`
- **السبب:** مسارٌ حيٌّ بلا رابط. يُبلَغ فقط من زرٍّ ثانويٍّ داخل المساعد (`needEngine.ts:EXPLORE`)، و`/feed` **بلا رابطٍ واحدٍ في التطبيق كلّه**.
- **الأثر:** بحثٌ موحّد + خريطةُ Leaflet + «ابحث في هذه المنطقة» + إدخالٌ صوتيّ — كلُّها تعمل ولا يصل إليها مستخدم.


**✅ أُصلح.** مجموعةُ «الاكتشاف» في القائمة: السوق · استكشف · الجديد.
`NavItem` صار يقبل `href` لأنّ هذه أسطحٌ عامّةٌ تعيش خارج `MainLayout` فلا
`setPage` يصلها. يحرسها اختبارٌ يُلزم كلَّ مسارٍ مُسجَّلٍ ببابٍ في الواجهة.
---

## ⑦ `push` يقول «وصل» بلا مشترِك

```
CHANNELS.push → routes/push.notifyUser(userId,…)
        ↓
subs.length === 0 → return مبكّرًا                    (لا إرسال)
        ↓
المحوّل يُرجع { sent: true }                          ❌ ادّعاء
```

- **أين ينكسر:** `server/lib/engines/notification.js` → `CHANNELS.push`
- **الشدّة:** `MEDIUM` · **الثقة:** `HIGH` — **شوهد حيًّا**: أُرجعت `✅ وصل` ولا اشتراكَ في القاعدة
- **السبب:** `notifyUser` لا تُرجع شيئًا يميّز «أُرسل» من «لا مشترِك»، والمحوّلُ يفترض النجاح.
- **الأثر:** تشخيصٌ يكذب بلطف. البوّابةُ والسجلُّ يقولان إنّ الإشعار وصل بينما لم يُرسَل شيء — وهو عينُ الخطأ الذي بُني محرّكُ الإشعارات لإزالته.

**✅ أُصلح (2026-08-02).** `notifyUser` تُرجع الآن
`{sent, delivered, subscriptions, reason?}`، والمحوّلُ **يقرأ** ولا يفترض.
**مُتحقَّقٌ حيًّا:** `push → ➖ no-subscription` بدل `✅ وصل`. وعند وجود مشترِكين
يُعاد `delivered/subscriptions` فيُعرَف كم وصل فعلًا لا مجرّد «نعم».

---

## ⑧ الولاء: خادمٌ كاملٌ بلا شاشة

```
تسليمُ الطلب → addLoyaltyPoints() ✅ → loyalty_points ✅ → GET /api/loyalty ✅ → شاشة ❌
```

- **أين ينكسر:** لا صفحةَ ولا مكوّن. `loyaltyAPI` معرَّفٌ في `src/services/api.ts` ولا يُستدعى إلّا رقمًا في بطاقة الزبون.
- **الشدّة:** `MEDIUM` · **الثقة:** `HIGH`
- **الأثر:** نقاطٌ تُمنَح ولا يراها أحدٌ ولا تُصرَف. برنامجُ ولاءٍ بلا ولاء.


**✅ أُصلح.** `LoyaltyPanel` في صفحة الزبائن: الرصيدُ والمستوى والتقدّمُ
للمستوى التالي وزرُّ الصرف. وعلى الخادم كان النقصُ أعمقَ من الشاشة:
`redeemLoyaltyPoints` موجودةٌ في طبقة القاعدة **بلا مسارٍ يستدعيها**
(نقاطٌ تُمنَح ولا تُصرَف أبدًا)، وكانت تقصّ الفائضَ بصمتٍ
(`GREATEST(0, points-$1)`) فيُطلَب صرفُ ١٠٠ من رصيد ٣٠ فيُصرَف ٣٠ ويُقال
«تمّ». الصرفُ الآن ذرّيٌّ شرطيّ: كلُّ المطلوب أو لا شيءَ مع سبب.
و`getLoyaltyAll` كانت تُرجع صفوفًا خامًا بلا اسمِ زبون — رقمٌ بجانب مُعرِّفٍ
لا يعرفه أحد. (`server/test/loyalty.test.js` — ٤)
---

## ⑨ «أنشطتي»: المخطّطُ يسمح والواجهةُ لا تعرض

```
users → providers (فهرسٌ غيرُ فريد على user_id) ✅ + provider_concepts ✅ → شاشةُ «أنشطتي» ❌
```

- **أين ينكسر:** لا صفحة. `PAGE_IDS` في `src/types.ts` بلا مدخل.
- **الشدّة:** `MEDIUM` · **الثقة:** `HIGH` — مُتحقَّقٌ من `pg_indexes`: `idx_providers_user` **غيرُ فريد**
- **الأثر:** «إنسانٌ واحدٌ بأنشطةٍ متعدّدة» مسموحٌ في البيانات وممنوعٌ في التجربة. المستخدمُ لا يرى أنشطتَه ولا يبدّل بينها.


**✅ أُصلح.** `MyActivities` في «ملفّي»: المتجرُ وكلُّ أنشطةِ الخدمات التي
يملكها الحساب، بحالة كلٍّ منها ورابطٍ إليه.

> **وما لم يُفعل عمدًا:** لا **مبدّلَ نشاط**. لا شيءَ في بقيّة التطبيق مُقيَّدٌ
> بنشاطٍ واحد — المنتجاتُ والطلباتُ كلُّها للحساب. فمبدّلٌ يوهم بعزلٍ لا وجودَ
> له كذبةٌ أكبرُ من الإخفاء.
---

## ⑩ القدرات: `CapabilityBar` بصفر استيراد

- **أين ينكسر:** `src/components/CapabilityBar.tsx` — لا يستورده أحد
- **الشدّة:** `LOW` · **الثقة:** `HIGH`
- **السبب:** مكوّنٌ يعرض قدراتِ الصفحة من الـAKG (القانون ٦) بُني ولم يُركَّب.
- **الأثر:** كلُّ صفحةٍ تُخفي ما تستطيعه؛ المستخدمُ يكتشف القدراتِ بالصدفة.


**✅ رُكِّب** في محرّر المنتج، حيث المعالجاتُ موجودةٌ فعلًا. وتغيّر عقدُه:
كان `onRun` **اختياريًّا**، فمن يُركّبه بلا معالجاتٍ يملأ صفحتَه أزرارًا لا
تفعل شيئًا — وهو عينُ العطب الذي بُني هذا المكوّن ليمنعه. المعالجاتُ الآن هي
القائمة: تُعرَض القدرةُ إن وُجد لها معالج، ولا تُعرَض إن لم يوجد. فلا يمكن
لهذا المكوّن أن يُنتج زرًّا ميّتًا بحكم بنيته.
---

## ⑪ خمسةُ مكوّناتٍ ميّتة

`BrandVideo` · `CommandCenter` · `EmptyState` · `KeyboardShortcuts` · `MapView`* — صفرُ استيرادٍ لكلٍّ منها.

- **الشدّة:** `LOW` · **الثقة:** `HIGH`
- **ملاحظة:** `MapView` **يُستورَد** بـ`lazy()` من `Explore.tsx` — فهو ليس ميّتًا، لكنّ صفحتَه بلا باب (⑥). و`CommandCenter` هو تنفيذُ `DR-0005` (الشريحةُ الأولى من «الواجهة المتدرّجة») — قرارٌ معتمَدٌ كُتب كودُه ولم يُركَّب.


**✅ حُسم — «يُركَّب أو يُحذَف».**

| المكوّن | القرار |
|---|---|
| `MapView` | لم يكن ميّتًا: `lazy()` من `Explore`. صفحتُه صار لها بابٌ (⑥). |
| `CommandCenter` | **رُكِّب** فوق «فضائي» — تنفيذُ `DR-0005`: «اليوم» قبل الميزات. فوقها لا بدلًا منها. |
| `KeyboardShortcuts` | **رُكِّب** في `MainLayout` — لا يرسم شيئًا حتى يُضغط «؟». |
| `CapabilityBar` | **رُكِّب** (⑩). |
| `EmptyState` | **حُذف** — مكوّنٌ عامٌّ لا يُفرّق بين «فراغِ التصفية» و«الفراغ الحقيقيّ»، وهو الفرقُ الذي كلّفنا عطبَين (⑬). تركُه دعوةٌ لتكرارهما. |
| `BrandVideo` | **حُذف** — بلا خطّةِ تركيبٍ ولا مستهلك. |

يحرسها اختبارٌ: أيُّ مكوّنٍ في `src/components` بلا مستورِدٍ يُسقط البناء
(ويقرأ `lazy(() => import(...))` كتركيبٍ صحيح، وإلّا قتل `MapView` ظلمًا).
---

## ⑫ الربحُ يُحسَب من مصدرين ويبتلع رسمَ التوصيل

- **أين:** `server/routes/analytics.js` · `src/pages/AnalyticsPage.tsx`
- **الشدّة:** `CRITICAL` — رقمٌ يقرّر عليه التاجرُ أسعارَه
- **الثقة:** `HIGH` — قُرئ من لقطةِ `/insights`: إيراد ١٬١٦٠ درهمًا، «صافي الربح −٨٨»، هامش −٨٪

**ثلاثةُ أعطابٍ في صيغةٍ واحدة:**

**(أ) رسمُ التوصيل إيرادٌ بلا مصروف.** `serverTotal = afterDiscount + delivery`
⇒ رسمُ التوصيل **داخلَ** `total`، فيُحسب إيرادًا. ولا يُطرح في أيّ مكان.
مالُ شركةِ التوصيل كان يظهر ربحًا صافيًا للتاجر.

**(ب) التكلفةُ لا تُلتقَط، بل يُعاد تسعيرُ الماضي بحاضرِه.** الثمنُ يُثبَّت في
البند (`safeItems.push({ ...it, price })`) والتكلفةُ **لا**. فالتحليلُ يقرأ
`products.find(...).cost` — تكلفةَ **اليوم**. يرفع التاجرُ ثمنَ الشراء في
بطاقةِ منتج، فينقلب ربحُ طلبٍ سُلِّم منذ شهرين خسارةً بأثرٍ رجعيّ.

**(ج) إيرادُ الخادم مطروحٌ منه تكلفةُ المتصفّح.**

```ts
const revenue   = serverData?.revenue ?? active.reduce(…)   // الخادم
const costTotal = active.reduce(…)                          // المتصفّح دائمًا
const profit    = serverData?.profit ?? (revenue - costTotal)
```

ومع ذلك كان الشريطُ الصغير تحت «صافي الربح» يرسم `rev - cost` المحلّيّة
وحدَها ⇒ الرقمُ والرسمُ تحته يمكن أن يتناقضا في نفس البطاقة.

**الإصلاح:** `server/lib/orderCosting.js` + نظيرُه `src/lib/orderCosting.ts` —
صيغةٌ واحدة: `الربح = الإيراد − ثمنُ الشراء المُلتقَط − رسمُ التوصيل`.
التكلفةُ تُثبَّت في البند وقتَ الطلب (`attachCosts`، لا تثق بالعميل ولا
بمنتجاتِ غيرِ صاحبِ المتجر)، والقديمُ بلا التقاطٍ يسقط على منتجِ اليوم كما كان.
النسختان محروستان بنفس الأمثلة: `server/test/order-costing.test.js` (٩) و
`test/brain/orderCosting.test.ts` (٧).

> **ما لم يُصلَح، ولا يُصلَح بالكود:** −٨٨ درهمًا كانت أيضًا تعني أنّ ثمنَ
> الشراء المُدخَل يفوق ثمنَ البيع في بعض البطاقات. الصيغةُ صارت صادقة؛ إن بقي
> الرقمُ سالبًا فهو خبرٌ عن التسعير، لا عن الكود. البطاقةُ تقول ذلك الآن صراحةً.

---

## ⑬ «لا توجد طلبات بعد» فوق ١٦ طلبًا

- **أين:** `src/pages/OrdersPage.tsx` · `src/pages/ProductsPage.tsx`
- **الشدّة:** `HIGH` — التطبيقُ يُنكر عملَ صاحبه
- **الثقة:** `HIGH` — لقطةُ `/orders`: «الكل ١٦ · شُحن ١٠ · جارٍ ٤» والشاشةُ تحتها فارغة

الصفحةُ تفتح على تبويب «بانتظار» (`useState('pending')`). تاجرٌ لا طلبَ معلّقًا
عنده يرى: **«لا توجد طلبات بعد — شارك رابط متجرك لاستقبال أوّل طلب»**، وفوقها
مباشرةً عدّادٌ يقول ١٦. الجملةُ تُنكر العملَ وتدعو لبدايةٍ قُطعت.

مثلُها في `/products`: `filtered.length === 0` ⇒ «لا توجد منتجات — ابدأ بإضافة
أوّل منتج»، وفي وضع الخدمات كانت تُنكر منتجاتٍ موجودةً **وتدعو لإضافة «منتج»
في صفحةِ خدمات**.

**الإصلاح:** الفراغان فُصلا. «لا شيءَ هنا» تعرض العددَ الكلّيَّ وزرَّ «عرض
الكلّ»؛ «لا شيءَ أبدًا» وحدَها تحتفظ بجملة البداية. وعدّاداتُ التبويبات صارت
تُظهر الصفرَ بدل إخفائه — الصفرُ المكتوبُ يشرح الفراغَ، والمخفيُّ يُبهمه.
وإن لم يكن في «بانتظار» شيءٌ ولم يختر التاجرُ تبويبًا، تُفتح الصفحةُ على «الكل».

يحرسها `test/architecture.test.mjs` — قاعدةٌ صنفيّةٌ لا إصلاحُ موضعين: أيُّ
جملةِ بدايةٍ تحت فراغِ قائمةٍ **مُصفّاة** تُسقط البناء.

---

## ⑭ «حجز» كلمةٌ لنظامين لا صلةَ بينهما

- **أين:** `src/pages/BookingsPage.tsx` مقابل `src/pages/ProductsPage.tsx:BookingsCalendar`
- **الشدّة:** `MEDIUM` — لا بياناتٍ تضيع، لكنّ التاجرَ يرى تناقضًا
- **الثقة:** `HIGH` — لقطتان: `/bookings` تقول «٠ حجوزات»، و`/services` تعرض بطاقاتٍ عليها «٤ حجز» و«١٣ حجز»

| | `/bookings` | تقويمُ `/services` |
|---|---|---|
| المصدر | جدولُ `bookings` + `providers` | بنودُ `orders` من نوع `service` |
| مَن يُنشئ | تسجيلُ مقدّمِ خدمةٍ خارجيّ | زبونٌ يطلب خدمةً من المتجر |
| العدّاد المعروض | `bookings.length` | `p.sales` — يزيد عند **الموافقة على طلب** |

النظامان صحيحان كلٌّ على حدة؛ الاسمُ وحدَه مشترك. البطاقةُ صارت تقول «٤ طلب»
(وهو ما تعدّه حرفيًّا)، وصفحةُ الحجوزات تشرح في فراغها أيَّ حجزٍ تعدّ وأين
يظهر الآخر.

**قرارٌ مؤجَّل للمالك:** دمجُ النظامين قرارُ منتجٍ لا إصلاحُ عطب. الوصفُ هنا
كافٍ لاتّخاذه.

---

## ⑮ سجلُّ التدقيق كاملٌ على الخادم ولا تقرؤه شاشتان

- **أين:** `src/store.tsx` · `src/pages/NotificationsPage.tsx` · `src/pages/SettingsPage.tsx`
- **الشدّة:** `HIGH` — «من غيّر هذا ومتى؟» سؤالٌ لا جوابَ له
- **الثقة:** `HIGH` — لقطةُ `/notifications`: «سجل النشاطات — لا نشاط بعد» فوق حسابٍ فيه ١٦ طلبًا

```
١٣ ملفَّ مسارٍ يستدعي db.addLog()        ✅ يُكتب
        ↓
جدولُ audit_logs (٥٠٠ صفٍّ محفوظة)       ✅ موجود
        ↓
GET /api/settings/logs                    ✅ يعمل
        ↓
settingsAPI.getLogs()                     ✅ معرَّفٌ في api.ts
        ↓
مَن يناديه؟  ← DeliveryPage وحدَها، ومُصفّاةً على type==='delivery'
        ↓
`auditLogs` في المخزن                     ❌ ذاكرةُ تبويبٍ فقط
```

`log()` في `store.tsx` يكتب في الحالة المحلّيّة ولا يُقرأ من الخادم أبدًا ⇒ كلُّ
تحديثِ صفحةٍ يمحو السجلَّ الظاهر. الشاشتان اللتان تعرضانه («سجل النشاطات» في
الإشعارات، و«سجلات التدقيق (٠)» في الإعدادات) تقولان «لا شيء» بينما القاعدةُ
مليئة. الحقيقةُ كانت موجودةً ولا تُقرأ.

**الإصلاح:** `refreshData` صار يجلب `settingsAPI.getLogs()` مع بقيّة البيانات
(داخل `allSettled` نفسِه، فسقوطُه لا يُسقط شيئًا)، والخادمُ هو المصدر.

---

## ⑯ «هل الذكاءُ متاح؟» يُجاب في أربعةِ مواضعَ بثلاثةِ أجوبة

- **أين:** `ChatImportPage` · `SystemCheck` · `MessagesPage` · `ProductsPage`
- **الشدّة:** `HIGH` — قدرةٌ مدفوعةُ الثمن تُنكَر على صاحبها
- **الثقة:** `HIGH` — لقطتان: `/connections` تُظهر Claude ✓ وDeepSeek ✓، و`/import` تحكم بمفتاحين فقط

| الموضع | كان يسأل | الخادم يقبل |
|---|---|---|
| `ChatImportPage:239` | `apiKey \|\| geminiKey` | ٦ مزوّدين |
| `SystemCheck:77` | `apiKey \|\| geminiKey` | ٦ |
| `MessagesPage:170` | `apiKey \|\| geminiKey` | ٦ |
| `ProductsPage:635` | الستّة ✅ | ٦ |

تاجرٌ موصولٌ بـClaude وDeepSeek وحدَهما — وكلاهما معروضٌ في «ربط الخدمات»،
وDeepSeek موسومٌ **«الأفضل للدارجة»** — يرى وصفَ المنتجات يعمل، ويُقال له في
ثلاثِ شاشاتٍ أخرى «بدون مفتاح AI». وكانت نقطةٌ خضراءُ نابضةٌ تعلو الجملةَ نفسَها.

**الإصلاح:** `src/lib/aiAvailability.ts` — `hasAI` · `aiProviders` ·
`aiProviderName`، وقائمةٌ هي قائمةُ `AI_PROVIDERS` في الخادم حرفيًّا.
و`hasImageAI` للسؤال الأضيق (توليدُ الصور: OpenAI · Gemini · Grok وحدَهم).
يحرسها `test/architecture.test.mjs`: القائمتان تُقارَنان، وأيُّ ملفٍّ يشتقّ
الحكمَ بنفسِه يُسقط البناء (مُختبَرٌ بالحقن من الجهتين).

---

## ⑰ محرّرُ الصور يُسقط اللوغو من التصدير

- **أين:** `src/pages/ImageEditorPage.tsx` → `exportImage()`
- **الشدّة:** `MEDIUM` — وعدُ الصفحةِ نفسِها ينكسر بصمت
- **الثقة:** `HIGH` — قراءةُ كود: `drawLayers` تعالج `logo`، و`finish()` لا تعالجه

عنوانُ الصفحة: «أضف **لوغو**، نص، وعناصر لمنتجاتك». المعاينةُ ترسم طبقةَ
اللوغو (`drawLayers` سطر ١٤٠)، والتصديرُ يمرّ على `text` و`sticker` فقط ⇒
التاجرُ يرى لوغوَه ثمّ ينزّل صورةً بلا لوغو.

وعطبٌ ثانٍ في الدالّة نفسِها: تحميلُ الخلفيّة في التصدير بلا `onerror` ⇒ صورةٌ
لا تُحمَّل تعني أنّ `finish()` لا تُستدعى أبدًا و**لا ينزل شيءٌ إطلاقًا**، بلا
رسالة. (المعاينةُ كان لها `onerror`؛ التصديرُ لا.)

**الإصلاح:** التصديرُ صار `async` بتحميلٍ يُنهي دائمًا، يرسم طبقاتِ اللوغو،
ويُخبر حين يسقط شيءٌ بدل أن يصمت. وزرُّ التصدير لم يعد ينزّل مربّعًا فارغًا.

---

## ⑱ «لا يوجد رابط» فوق شركةٍ موصولة

- **أين:** `src/pages/DeliveryPage.tsx:868`
- **الشدّة:** `LOW` — عرضٌ مضلّلٌ لا عطبٌ وظيفيّ · ✅ أُصلح
- **الثقة:** `HIGH` — قراءةُ كودٍ مع لقطةِ `/delivery`

السطرُ يقرأ `p.websiteUrl` وحدَه ويكتب «API · لا يوجد رابط» فوق شركةٍ موصولةٍ
بـ`apiEndpoint` يعمل (Livo، ٤٤١ مدينة). الرابطُ المعروضُ صار الرابطَ
المُستعمَلَ فعلًا: `apiEndpoint` في وضع API، و`websiteUrl` في وضع الأتمتة.

---

## ⑲ استوديو البانر يقرأ توكنًا لا وجودَ له

- **أين:** `src/pages/BannerStudioPage.tsx:89`
- **الشدّة:** `HIGH` — صفحةٌ كاملةٌ لا تعمل، وتصمت
- **الثقة:** `HIGH` — قراءةُ كود؛ كشفها حارسُ مفاتيح التخزين

```js
const tok = localStorage.getItem('token') || '';     // ← مفتاحٌ لا وجودَ له
fetch('/api/ai/reply', { headers: { Authorization: `Bearer ${tok}` } })
```

التوكنُ في هذا التطبيق يعيش **في الذاكرة** ويُقرأ بـ`getToken()`؛ و
`ai_commerce_token` لا يُستعمل إلّا لوضع العرض. فكلُّ نداءٍ من هذه الدالّة
يُرسَل بترويسةٍ فارغةٍ ويُرَدّ ٤٠١.

والأدهى: **الدالّةُ الأخرى في الملفّ نفسِه (سطر ٦٣) تستعمل `getAuthToken()`
بشكلٍ صحيح.** أُصلح نصفُ الملفّ يومًا ما ولم يُصلح النصفُ الآخر.

---

## ⑳ محرّرُ الصور يستقبل منتجًا لا يُرسله أحد

- **أين:** `src/pages/ImageEditorPage.tsx` ⟷ `src/pages/ProductsPage.tsx`
- **الشدّة:** `MEDIUM` — نصفُ ميزةٍ مبنيٌّ ولا يُستعمل
- **الثقة:** `HIGH` — كشفها الحارسُ نفسُه

المحرّرُ يقرأ ثلاثةَ مفاتيحَ من الجلسة عند فتحه:
`amanzine_editor_image` · `editor_action` · `editor_store_name`،
وتعليقُه يقول «Handle action from **ProductsPage**». **لا سطرَ في التطبيق كلِّه
يكتب أيًّا منها.** الطرفُ المُستقبِل كاملٌ منذ البداية والطرفُ المُرسِل مفقود،
فتُفتح الصفحةُ على قماشٍ فارغٍ دائمًا — وهو ما ظهر في لقطة `/editor`.

**الإصلاح:** زرُّ «🎨 صورة» في بطاقة المنتج يكتب المفاتيحَ الثلاثةَ وينتقل
للمحرّر. الميزةُ صارت كاملة.

> **ثقبٌ في الحارس نفسِه، سُدّ:** كان يعدّ `removeItem` كتابةً، فمرّ
> `editor_action` (يُقرأ ويُمسَح ولا يُكتب) بلا اكتشاف. المسحُ لا يملأ عدّادًا.

---

## ㉑ «الثقة ٨٢٪» تحت درعٍ — وهي اكتمالُ ملفٍّ ذاتيّ

- **أين:** `src/pages/ProfilePage.tsx`
- **الشدّة:** `MEDIUM` — إشارةُ سمعةٍ مُفبركة
- **الثقة:** `HIGH` — لقطةُ `/profile`

الرقمُ يُحسب هكذا: `٤٠ + ١٢ إن كان للمتجر اسم + ٨ إن كان له شعار + ١٥ إن كان
فيه منتجات…` — أي **عدُّ حقولٍ ملأها صاحبُ الحساب بنفسِه**. ويُعرَض تحت
`ShieldCheck` باسم «الثقة»، فيقرؤه صاحبُه ومَن يراه على أنّه تحقّقٌ من المنصّة.
ولا نظامَ سمعةٍ على الخادم لصاحب الحساب أصلًا (`trust_score` للزبائن لا للتاجر).

وفي الصفحة نفسِها عطبان أصغر:
- **«مفضّلتي»** تقرأ `amanzine_favorites` — مفتاحًا **لا يكتبه أحد**. صفرٌ أبديٌّ
  معروضٌ كإحصائيّة، وشريطُ التنقّل يعِد بها. أُبدلت بعددٍ حقيقيٍّ من
  `experienceLog`.
- **«طلباتي»** كانت تعدّ طلباتِ المتجر المُستلَمة، لا طلباتٍ اشتراها صاحبُ
  الحساب. صارت «طلبات متجري».

**الإصلاح:** الرقمُ نفسُه بقي، واسمُه صار «اكتمالُ الملفّ». «الثقة» محجوزةٌ
لسمعةٍ حقيقيّةٍ حين تُبنى على الخادم.

---

## سلاسلُ فُحصت ووُجدت **سليمة**

أُدرَجت لأنّ «ما لم يُذكَر» يُظنّ معطوبًا:

| السلسلة | التحقّق |
|---|---|
| تسجيل → منتج → طلب → إشعار → سجلّ | ✅ حيًّا |
| `order.created` → الناقل → `learning_daily` | ✅ حيًّا (`stage=order, count=1`) |
| حفظُ شركةِ توصيلٍ بلا نوع → استدلال → `api_type='livo'` → حلُّ السجلّ | ✅ حيًّا |
| `provider:<id>` → مالكٌ → إشعارٌ داخليّ | ✅ حيًّا |
| واتساب غيرُ مهيّأ → رابطُ محادثةٍ يدويّة بديل | ✅ حيًّا |
| الترحيل → ٤٠ جدولًا | ✅ حيًّا |
| `GET /api/search` → نيّة + مرشّحات + أوزان | ✅ حيًّا |
| السجلُّ يمسح المجلّد → ٣ مزوّدين · ٠ مرفوض | ✅ حيًّا |
