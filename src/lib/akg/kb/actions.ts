// ============================================================
// محرّكُ الأفعال — «شنو بغيتي **ندير**؟» لا «شنو بغيتي **تشري**؟»
//
//   سؤالُ المالك حرفيًّا: كيف يعرف التطبيقُ أنّ «بغيت نبدل رقم الهاتف» إعدادٌ،
//   و«بغيت نبدل الثمن» منتوج، و«بغيت نزيد محل» نشاطٌ جديد — وكلُّها تبدأ
//   بـ«بغيت»؟
//
//   والجوابُ ليس ذكاءً جديدًا، بل **فصلُ ما كان مختلطًا**. الجملةُ الواحدة
//   تحمل ثلاثةَ أشياءَ لا شيئًا واحدًا:
//
//       بغيت نبدل    رقم الهاتف    ديالي
//         ↑             ↑            ↑
//        فعل          هدف         نطاق
//
//   فإن فُصلت صارت مصفوفةً صغيرةً مقروءة، وإن بقيت مختلطةً احتجتَ قاعدةً
//   لكلّ جملةٍ ممكنةٍ في الدنيا.
//
//   ── لماذا هنا لا في `facts.ts` ──
//   `facts.ts` يقرأ ما يسأل عنه **الزبون** عن بضاعةٍ معروضة (الثمن، المقاس،
//   التوصيل). وهذا يقرأ ما يريد **صاحبُ الحساب** أن يفعله بتطبيقه. نفسُ
//   النمط، ومجالان لا يصحّ خلطُهما: «شحال الثمن؟» سؤالُ زبون، و«بدّل الثمن»
//   أمرُ تاجر.
//
//   ── وما لا يفعله هذا الملفّ ──
//   لا يُنفّذ شيئًا. يقرأ فقط، ويقول **ما ينقص** ليُنفَّذ. فمن يقول «بدّل
//   الثمن» لم يقل ثمنَ أيّ منتج — والجوابُ الصحيح سؤالٌ واحد لا تخمين،
//   وهي نفسُ قاعدة `ambiguity.ts`: **اسأل، ولا تُخمّن**.
// ============================================================

import { deArabizi } from './arabizi';
import { normLoose, hitsWord } from '../../normalize';
import { readAmount } from '../../money';

/** ما يريد فعلَه. */
export type ActionVerb = 'view' | 'create' | 'update' | 'delete' | 'share' | 'send';

/** بماذا. */
export type ActionObject =
  | 'phone' | 'language' | 'password' | 'account'      // إعداداتُ الشخص
  | 'workspace' | 'shop_name' | 'shop_hours' | 'address' | 'delivery' | 'settings'  // إعداداتُ النشاط
  | 'product' | 'price' | 'stock' | 'photo' | 'content' // الكتالوج
  | 'channel'                                          // قنواتُ التواصل المربوطة
  | 'shipment'                                         // شحنةٌ تُنشأ عند شركة
  | 'orders' | 'customers' | 'coupon' | 'wallet';      // العمل

/** لمن يعود الهدف: الشخصُ أم نشاطُه؟ */
export type ActionScope = 'user' | 'workspace';

export interface ActionRead {
  verb: ActionVerb;
  object: ActionObject;
  scope: ActionScope;
  /** ما ينقص ليُنفَّذ — تُطرَح سؤالًا واحدًا، لا استمارة. */
  needs: string[];
  /**
   * **الإشارةُ: قال أيَّ واحدٍ يقصد، بغير اسمِه.**
   *
   *   «مسح **هاد** المنتوج» · «بدّل ليا ثمن **آخر** منتوج» — كلامٌ صريحٌ
   *   يعيّن المقصود، وكان يُقابَل بـ«أيّ منتوج؟». والسؤالُ عمّا قيل للتوّ
   *   أسوأُ من السؤال عمّا لم يُقَل: الأوّلُ يُشعر الإنسانَ أنّه لم يُسمَع.
   *
   *   وتُقرأ هنا **مؤشِّرًا لا اسمًا**: هذه الطبقةُ تقرأ اللغةَ ولا تعرف
   *   كتالوجَ أحد. مَن يملك الكتالوج هو من يحلّها — ولو حلَلناها هنا
   *   لصار المعجمُ يقرأ قاعدةَ البيانات.
   */
  ref?: 'this' | 'last';
  /**
   * **حالةُ الطلب المقصودة** — «وافق» و«رفض» و«سلّم» ثلاثةُ أفعالٍ لا فعلٌ.
   *
   *   جردُ الصفحات كشفها: `approveOrder` و`rejectOrder` و`deliverOrder`
   *   أفعالٌ يملكها التاجرُ في صفحة الطلبات ويضغطها كلَّ يوم — ولم تكن
   *   في اللغة كلمةٌ واحدةٌ تبلغها. «وافق على الطلب» ⇒ «ما فهمتش».
   *
   *   وكلُّها `update` على `orders`، فلو اكتُفي بالفعل لاختلطت الموافقةُ
   *   بالرفض — وهما نقيضان على مالِ الناس. فتُقرأ الحالةُ صراحةً.
   */
  status?: 'approved' | 'rejected' | 'delivered';
  confidence: number;
  /** لماذا قُرئت هكذا — «كلُّ شيءٍ قابل للشرح». */
  reason: string;
}

const norm = normLoose;   // كان نسخةً محلّيّةً أضعف

// ── الأفعال ───────────────────────────────────────────────────
// «بغيت» وحدَها ليست فعلًا إداريًّا: «بغيت نبيع» تجارةٌ لا إعداد. فلا تدخل
// هنا — يلتقطها `humanIntent` بعدَنا. الفعلُ هنا **صريحٌ في التصرّف**.
const VERBS: { verb: ActionVerb; terms: string[] }[] = [
  // «مسح» مجرّدةً كانت غائبةً و«امسح»/«نمسح» حاضرتَين — والمغربيُّ يقول
  // «مسح المنتوج» أمرًا مختصرًا بلا همزةٍ ولا نون.
  { verb: 'delete', terms: ['حيد', 'حيّد', 'نحيد', 'حذف', 'نحذف', 'مسح', 'امسح', 'نمسح', 'الغي', 'نلغي', 'سد', 'نسد', 'supprimer'] },
  // ودورةُ حياة الطلب تبديلُ حالة: «وافق» · «رفض» · «سلّم». أكثرُ ما يضغطه
  //   التاجرُ في يومه، ولم تكن في اللغة كلمةٌ تبلغه — «وافق على الطلب» تُقابَل
  //   بـ«ما فهمتش». والحالةُ المقصودةُ تُقرأ بعدُ في `readOrderStatus`.
  { verb: 'update', terms: ['بدل', 'بدّل', 'نبدل', 'غير', 'غيّر', 'نغير', 'صحح', 'نصحح', 'عدل', 'نعدل', 'حدث', 'نحدث',
                            'وافق', 'نوافق', 'قبل الطلب', 'اقبل', 'صادق', 'أكّد الطلب', 'اكد الطلب',
                            'رفض', 'نرفض', 'ارفض', 'لغي الطلب', 'الغي الطلب',
                            'سلم', 'سلّم', 'تسلم', 'توصل', 'وصل للزبون', 'تسلمو',
                            'modifier', 'changer'] },
  { verb: 'create', terms: ['زيد', 'نزيد', 'ضيف', 'نضيف', 'اضف', 'صايب', 'نصايب', 'دير ليا', 'ندير واحد', 'جديد', 'ajouter', 'creer',
  // الربطُ إنشاءُ وصلٍ — «بغيت نربط الواتساب ديالي بالحساب».
  //   و«وصل» المجرّدةُ **مُخرَجةٌ**: قِيست فابتلعت «فين **وصلات** الكوموند»
  //   فصار سؤالُ التتبّع طلبَ إنشاء. نفسُ مصرف الكلمة العامّة، في قائمة
  //   الأفعال هذه المرّة — والعبارةُ الكاملةُ تكفي.
                            'ربط', 'نربط', 'اربط', 'كنربط', 'نوصل بالحساب', 'connecter', 'lier'] },
  // «بارطاجي» تعريبُ `partager` — وهي أشيعُ من «شارك» في الدارجة حين
  //   يُقصَد النشرُ على شبكةٍ اجتماعيّة. وكانت «بغيت نبارطاجي هدشي
  //   فالفيسبوك» تُقرأ **عرضَ خدمة** ⇒ «شنو نوع النشاط؟».
  { verb: 'share',  terms: ['شارك', 'نشارك', 'صيفط الرابط', 'رابط المحل',
                            'بارطاجي', 'نبارطاجي', 'برطاجي', 'نشير', 'شير', 'partager'] },
  // و«سيفط» بالسين كما «صيفط» بالصاد — الدارجةُ تُبدّلهما، والمغربيُّ
  //   يكتب الاثنتَين. وكانت «بغيت **نسيفط** لبنتي واحد الأمانة» بلا فعلٍ
  //   إطلاقًا لأنّ السينَ وحدَها غيرُ معروفة.
  { verb: 'send',   terms: ['صيفط', 'نصيفط', 'سيفط', 'نسيفط', 'ارسل', 'نرسل', 'envoyer'] },
  // «شحال من» و«شحال عندي» سؤالُ عددٍ — وهو **عرضٌ** لا سؤالٌ عن قدرة.
  //   «وريني الطلبات» كانت تُقرأ و«شحال من طلب عندي» لا، وهما طلبٌ واحد:
  //   أرِني ما عندي. والمغربيُّ يسأل بالعدد أكثرَ ممّا يأمر بالعرض.
  //   و«فين وصلات» سؤالُ تتبّعٍ — وهو عرضٌ أيضًا. وكانت جملةُ الزبون
  //   «فين وصلات الكوموند ديالي» تُقرأ **«شنو نوع النشاط ولا الخدمة؟»**:
  //   مَن يسأل عن طرده يُسأل عن حرفته. أسوأُ جوابٍ في الثلاثَ عشرةَ كلِّها.
  { verb: 'view',   terms: ['وريني', 'ورينا', 'شوف', 'نشوف', 'بين ليا', 'عرض', 'اعرض', 'فين كاين', 'افتح', 'نفتح',
                            'فين وصلات', 'فين وصل', 'فين كاينه', 'واش وصل',
                            'شحال من', 'شحال عندي', 'شحال عندنا', 'كم من', 'combien', 'voir', 'afficher',
  // **وسؤالُ الثمن عرضٌ لا بحث.** «شحال ثمن التوصيل» كانت تُقرأ `find_pro`
  //   ⇒ «شنو محتاج بالضبط؟» — أي يُساق إلى البحث عن حرفيٍّ مَن يسأل عن رقمٍ
  //   عندنا. وحدُّها ضيّق: «شحال» متبوعةً بثمنٍ أو كلفة، لا «شحال» وحدَها
  //   (فهي في «شحال هادا؟» سؤالُ زبونٍ عن بضاعةٍ معروضة — بابٌ آخر).
                            'شحال ثمن', 'شحال كيسوا', 'شحال كيكلف', 'شحال تمن', 'بشحال التوصيل',
  // **والطردُ الراجعُ حالةٌ يُنظَر فيها لا خدمةٌ تُعرَض.** «عندي طرد راجع»
  //   لا فعلَ فيها منطوقًا، فكانت تسقط إلى `create_service` ⇒ صفحةُ النشر.
  //   والعباراتُ هنا كاملةٌ لا كلماتٌ مفردة: «عندي» وحدَها ملكيّةٌ لا نظر،
  //   وتوسيعُها يجعل كلَّ «عندي محل» طلبَ عرض.
                            'عندي طرد راجع', 'عندي كولي رتور', 'عندي مرتجع', 'عندي طرد رجع',
                            'جاني طرد راجع', 'رجع ليا الطرد', 'رجعو ليا الطرد'] },
];

// ── الأهداف ───────────────────────────────────────────────────
// الترتيبُ من الأخصّ إلى الأعمّ — **جزءٌ من المعنى**: «رقم الهاتف» هدفٌ
// مستقلّ، ولو سبقته «الإعدادات» العامّةُ لابتلعته.
const OBJECTS: { object: ActionObject; scope: ActionScope; terms: string[]; needs?: string[] }[] = [
  // إعداداتُ الشخص
  { object: 'phone',    scope: 'user', terms: ['رقم الهاتف', 'رقم التيليفون', 'النمرة', 'رقمي', 'الهاتف ديالي', 'numero'] },
  { object: 'password', scope: 'user', terms: ['كلمة السر', 'كلمه السر', 'الباسوورد', 'mot de passe', 'password'] },
  { object: 'language', scope: 'user', terms: ['اللغه', 'اللغة', 'لغه التطبيق', 'بالعربيه', 'بالفرنسيه', 'langue'] },
  { object: 'account',  scope: 'user', terms: ['الحساب ديالي', 'حسابي', 'الكونط ديالي', 'mon compte'] },

  // النشاط
  // **الأخصُّ قبل الأعمّ — والملفُّ يقول ذلك ثمّ يخالفه.** كان `workspace`
  //   أوّلًا، وفيه «المحل». فـ«بدّل **اسم المحل**» تُقرأ `update/workspace`
  //   ويضيع «الاسم» — أي تُفتَح إعداداتُ المحلّ كلُّها لمن سمّى حقلًا واحدًا.
  //   ولم يظهر ذلك قبلَ اليومَ لأنّ الوجهةَ كانت صفحةً في الحالتَين.
  { object: 'shop_name',  scope: 'workspace', terms: ['اسم المحل', 'سميه المحل', 'اسم المتجر'] },
  { object: 'shop_hours', scope: 'workspace', terms: ['وقت الخدمه', 'ساعات العمل', 'وقتاش كنحل', 'التوقيت'] },
  { object: 'workspace',  scope: 'workspace', terms: ['محل', 'المحل', 'متجر', 'المتجر', 'حانوت', 'الحانوت', 'نشاط', 'boutique', 'magasin'] },
  // **العنوانُ كان قدرةً بلا كلمة.** `CHANGE_ADDRESS` مُعلَنةٌ منذ زمن و
  //   `brand.address` يُحفَظ ويُعرَض للزبون ويدخل في حساب التوصيل — ولم يكن
  //   في قائمة الأهداف هدفٌ اسمُه «عنوان». فـ«بغيت نبدل العنوان» تُقرأ
  //   `update/settings` ⇒ **«شنو بغيتي تبدّل؟»** لمن سمّاه للتوّ.
  { object: 'address',    scope: 'workspace', terms: ['العنوان', 'العنوان ديال المحل', 'لادريس', 'adresse'] },
  { object: 'delivery',   scope: 'workspace', terms: ['التوصيل', 'شركه التوصيل', 'ثمن التوصيل', 'livraison'] },
  // **وربطُ الواتساب فعلٌ إداريٌّ وله صفحةٌ تعمل** (`ConnectionsPage`).
  //   كان «بغيت نربط الواتساب ديالي بالحساب» يُقرأ **عرضَ خدمة** ويُساق إلى
  //   النشر. طبقةٌ تعمل ولا لغةَ تبلغها.
  { object: 'channel',    scope: 'workspace', terms: ['الواتساب', 'واتساب', 'whatsapp', 'الانستگرام', 'انستگرام', 'instagram',
                            'الفيسبوك', 'فيسبوك', 'facebook', 'القنوات', 'الربط بالحساب'] },

  // الكتالوج — الثمنُ والمخزونُ يحتاجان **نسخة**، فيُعلَن نقصُها صراحةً.
  // المجرّدُ بلا «ال» كان غائبًا هنا أيضًا: «بدّل ليا **ثمن** آخر منتوج»
  // كانت تُقرأ هدفَها `product` — فيُقال «بدّل منتوجًا» لمن قال «بدّل ثمنًا»،
  // ويسقط معها سؤالُ «بشحال؟». و«ثمن التوصيل» محفوظةٌ لأنّ `delivery` أسبقُ
  // في هذه القائمة، والترتيبُ من الأخصّ إلى الأعمّ جزءٌ من المعنى.
  { object: 'price',   scope: 'workspace', terms: ['الثمن', 'التمن', 'ثمن', 'تمن', 'السوم', 'سوم', 'السعر', 'سعر', 'الاثمنه', 'prix'], needs: ['أيّ منتج؟'] },
  { object: 'stock',   scope: 'workspace', terms: ['المخزون', 'الستوك', 'الكميه', 'stock'], needs: ['أيّ منتج؟'] },
  // المجرّدُ بلا «ال» كان غائبًا: «صايب **تصويرة** لهاد المنتوج» تُقرأ
  // `create/product` ⇒ «أيّ منتوج؟» — وهو يريد صورةً لمنتوجٍ **أشار إليه**.
  // و`POST /api/ai/design-product-image` يعمل ويُنادى من `ProductsPage`.
  { object: 'photo',   scope: 'workspace', terms: ['الصوره', 'التصويره', 'الصور', 'تصويره', 'صوره', 'تصاور', 'photo', 'image'], needs: ['أيّ منتج؟'] },
  // نصُّ المنتوج: وصفٌ وهاشتاگ. `POST /api/ai/generate-description` و
  // `/generate-hashtags` يعملان ويُناديان من `ProductsPage` — ولم يكن في
  // اللغة كلِّها كلمةٌ تبلغهما. «باغي هاشتاگ ولا وصف زوين» ⇒ «شنو محتاج
  // بالضبط؟» وكأنّه يقلّب على حرفيّ.
  { object: 'content', scope: 'workspace', terms: ['هاشتاگ', 'هاشتاق', 'هاشتاك', 'الوصف', 'وصف', 'كليمات مفتاح', 'hashtag', 'description'], needs: ['أيّ منتج؟'] },
  { object: 'product', scope: 'workspace', terms: ['منتوج', 'المنتوج', 'المنتج', 'سلعه', 'produit'] },

  // العمل
  // المفردُ يلزم مع سؤال العدد: «شحال من **طلب** عندي» — والمغربيُّ يُفرد
  // بعد «شحال من» دائمًا. وكان الجمعُ وحدَه مُعلَنًا، فسقط نصفُ الصيغة.
  // **والطردُ الراجعُ طلبٌ لا خدمةٌ تُعرَض.** «عندي طرد راجع» و«عندي كولي
  //   رتور» كانتا تُقرآن `create_service` ⇒ صفحةُ النشر: مَن عنده إرجاعٌ
  //   يُساق ليَعرض حرفة. وحالةُ «مرتجع» مُعلَنةٌ في دورة حياة الطلب
  //   (`deliveryStatus`) — فالكيانُ قائمٌ ولم تكن له كلمة.
  { object: 'orders',    scope: 'workspace', terms: ['الطلبات', 'الطلبيات', 'كوموند', 'طلب', 'طلبية', 'commandes', 'commande',
                            'طرد راجع', 'الطرد الراجع', 'طرد رجع', 'كولي رتور', 'كولي راجع', 'مرتجع', 'المرتجعات', 'retour',
                            'الطرد', 'طردي', 'الكولي', 'كولي', 'colis',
  // «أمانة» — طردٌ يُرسَل مع سائقٍ أو شركة، وهي أشيعُ ما يقوله المغربيّ.
  //   تنضمّ إلى هذه القائمة ولا تُفتَح لها قائمةٌ ثانية: كتبتُ أوّلًا هدفًا
  //   مستقلًّا `shipment` فيه «كولي» و«الطرد» — وهي هنا أصلًا — فسبقه في
  //   الترتيب وسرق «عندي كولي رتور» من `orders`. قائمتان لكلمةٍ واحدةٍ
  //   تتباعدان، وأسقطني حارسٌ قائمٌ في أوّل تشغيل.
                            'امانه', 'الامانه', 'باكيه'] },
  { object: 'customers', scope: 'workspace', terms: ['الزبناء', 'الزباين', 'العملاء', 'زبون', 'كليان', 'clients', 'client'] },
  { object: 'coupon',    scope: 'workspace', terms: ['كوبون', 'الكوبونات', 'تخفيض', 'خصم', 'promo'] },
  { object: 'wallet',    scope: 'workspace', terms: ['المحفظه', 'الرصيد', 'الفلوس ديالي', 'portefeuille'] },

  // الأعمُّ أخيرًا: تُصيب حين لا يُصيب هدفٌ أخصّ.
  { object: 'settings',  scope: 'user', terms: ['الاعدادات', 'الاعداد', 'parametres', 'reglages'] },
];

const hits = (t: string, terms: string[]) => terms.find(w => t.includes(norm(w)));

/**
 * يقرأ فعلًا إداريًّا من جملة، أو `null` إن لم تكن كذلك.
 *
 *   `null` هي الجوابُ الصحيح لأغلب الجمل: «بغيت نبيع قميص» تجارةٌ لا إعداد،
 *   وتُترَك لمن بعدَنا. محرّكٌ يدّعي فهمَ كلّ شيءٍ يفسد أكثرَ ممّا يُصلح.
 */
export function readAction(raw: string): ActionRead | null {
  const t = norm(deArabizi(raw || ''));
  if (!t) return null;

  const v = VERBS.find(x => hits(t, x.terms));

  // ── **هدفٌ يُولَّد بلا فعلٍ منطوق** ─────────────────────────────
  //
  //   «باغي هاشتاگ ولا وصف زوين لهاد المنتوج» — لا فعلَ فيها إطلاقًا:
  //   «باغي» مستثناةٌ عمدًا (وهي نيّةٌ تجاريّةٌ في أغلب الجمل). فكانت تُقرأ
  //   `find_pro` ⇒ «شنو محتاج بالضبط؟» — وكأنّه يقلّب على حرفيّ، وهو يطلب
  //   نصًّا يصنعه التطبيقُ له بزرٍّ قائمٍ في `ProductsPage`.
  //
  //   والقاعدةُ ضيّقةٌ عمدًا: **طلبُ شيءٍ يصنعه التطبيقُ هو طلبُ صنعِه**، ولا
  //   تسري إلّا على هدفَين يُولَّدان (`content` · `photo`). ولا تسري على
  //   «المنتوج» ولا «الطلبات» ولا شيءٍ يُعرَض أو يُملَك — فمن ذكر منتوجًا لم
  //   يطلب إنشاءَه. وثقتُها دون المنطوق (٠٫٦٠) لأنّ الفعلَ مُستنتَجٌ لا مقول.
  const GENERATED: ActionObject[] = ['content', 'photo'];
  if (!v) {
    const g = OBJECTS.find(x => GENERATED.includes(x.object) && hits(t, x.terms));
    if (!g) return null;
    return {
      verb: 'create', object: g.object, scope: g.scope,
      needs: [...(g.needs || [])], ref: readRef(t), confidence: 0.6,
      reason: `«${hits(t, g.terms)}» — شيءٌ يصنعه التطبيق، فطلبُه طلبُ صنعِه`,
    };
  }
  const vTerm = hits(t, v.terms)!;

  const o = OBJECTS.find(x => hits(t, x.terms));
  if (!o) {
    // فعلٌ بلا هدفٍ معروف. وهنا يفترق صنفان:
    //
    //   «بدّل» وحدَها ⇒ أمرُ إعدادٍ نقص هدفُه ⇒ نسأل «شنو بغيتي تبدّل؟».
    //   «وريني» وحدَها ⇒ **ليست أمرَ إعدادٍ أصلًا**. «وريني كساوي اللي عندك»
    //   جملةُ زبونةٍ تطلب بضاعة — قِيست من زيارةٍ ميدانيّة. ولو ادّعيناها
    //   إعدادًا لفتحنا للزبونة شاشةَ إعداداتٍ بدل أن نُريها الكساوي.
    //
    // فالأفعالُ العامّةُ في الكلام (عرضٌ وإرسالٌ ومشاركة) تصمت بلا هدف،
    // والأفعالُ التصرّفيّةُ (تعديلٌ وحذفٌ وإنشاء) تسأل.
    const ACTS_ON_DATA: ActionVerb[] = ['update', 'delete', 'create'];
    if (!ACTS_ON_DATA.includes(v.verb)) return null;
    return {
      verb: v.verb, object: 'settings', scope: 'user',
      needs: [`شنو بغيتي ${v.verb === 'delete' ? 'تحيّد' : v.verb === 'create' ? 'تزيد' : 'تبدّل'}؟`],
      confidence: 0.35,
      reason: `فعلٌ «${vTerm}» بلا هدفٍ واضح`,
    };
  }
  const oTerm = hits(t, o.terms)!;

  // ── **القناةُ «أين» لا «ماذا»** ────────────────────────────────
  //
  //   «بغيت نبارطاجي هدشي **فالفيسبوك**» — الفيسبوكُ موضعُ النشر، والمنشورُ
  //   هو المحلُّ أو المنتوج. فلو بقي الهدفُ `channel` لَما طابقت قدرةٌ
  //   (لا `share:channel` في الكتالوج) فتسقط الجملةُ إلى النيّة الخشنة
  //   ⇒ «شنو نوع النشاط ولا الخدمة؟» لمن يريد أن يشارك رابطًا.
  //
  //   والحدُّ ضيّق: **مع فعل المشاركة وحدَه**. أمّا «بغيت نربط الواتساب»
  //   فالقناةُ فيها هي المقصودةُ نفسُها، وتبقى كما هي.
  //   ونفسُ القاعدة مع الإرسال: «**صيفط** الطلب» و«**نسيفط** الأمانة»
  //   ليستا نظرًا في الطلبات بل **شحنةً تُنشأ عند شركة**. فالفعلُ يُبدّل
  //   ما يعنيه الهدفُ — كما تُبدّله المشاركةُ مع القناة.
  const object = (v.verb === 'share' && o.object === 'channel') ? 'workspace' as ActionObject
    : (v.verb === 'send' && o.object === 'orders') ? 'shipment' as ActionObject
    : o.object;
  const scope = (object === o.object) ? o.scope : 'workspace' as ActionScope;

  // الحذفُ يحتاج تأكيدًا دائمًا — لا يُنفَّذ من جملةٍ واحدة مهما وضحت.
  const needs = [...(o.needs || [])];
  if (v.verb === 'delete') needs.push('تأكيد: هذا لا يُسترجَع');

  // **ومن يبدّل ثمنًا لم يقل الثمنَ الجديد ينقصه ثمن.**
  //   كان نقصُ الثمن مستورًا بنقصِ المنتوج: يُسأل «أيّ منتوج؟» فلا يبلغ
  //   التنفيذَ أصلًا. فلمّا صارت الإشارةُ تُحَلّ («ثمن **آخر** منتوج») ظهر
  //   الأوّلُ عاريًا: تبديلُ ثمنٍ **بلا ثمن**. وسترُ عطبٍ بعطبٍ ليس إصلاحًا.
  if (object === 'price' && readAmount(raw) === undefined) needs.push('بشحال؟');

  const ref = readRef(t);
  const status = object === 'orders' ? readOrderStatus(t) : undefined;
  return {
    verb: v.verb, object, scope, needs, ref, status,
    confidence: needs.length ? 0.7 : 0.85,
    reason: `«${vTerm}» + «${oTerm}»${ref ? ` + إشارة «${ref === 'this' ? 'هاد' : 'آخر'}»` : ''}`,
  };
}

/**
 * حالةُ الطلب المقصودة — والنقيضان لا يُخلَطان.
 *
 *   «وافق» و«رفض» يبدّلان حالةَ الطلب نفسِه، فلو قُرئا فعلًا واحدًا
 *   (`update/orders`) لصار قبولُ الطلب ورفضُه شيئًا واحدًا — وهما نقيضان
 *   على مالِ الناس وعلى سلعةٍ تُشحَن.
 */
function readOrderStatus(t: string): 'approved' | 'rejected' | 'delivered' | undefined {
  //   والمطابقةُ بـ`hitsWord` لا بـ`\b`: حدُّ الكلمة في JavaScript مبنيٌّ على
  //   `[A-Za-z0-9_]` — والعربيّةُ ليست منه، فـ`\b` تسلك سلوكًا لا يُعتمَد
  //   عليه. قِيس فأعطى «حالة=—» لكلّ جملةٍ منها. وهذا نفسُ المطابِق الذي
  //   بُني لحدّ الكلمة العربيّة في `normalize.ts`.
  const has = (...ws: string[]) => ws.some(w => hitsWord(norm(w), t));
  // الرفضُ أوّلًا: «ما نوافقش» تحمل «نوافق» ولا تعني الموافقة.
  if (/ما\s*نوافقش/.test(t) || has('رفض', 'نرفض', 'ارفض', 'لغي', 'الغي', 'نلغي')) return 'rejected';
  if (has('وافق', 'نوافق', 'اقبل', 'قبل', 'صادق', 'اكد')) return 'approved';
  if (has('سلم', 'تسلم', 'تسلمو', 'توصل', 'وصل')) return 'delivered';
  return undefined;
}

/**
 * أيُّ واحدٍ يقصد — «هاد» ما بين يدَيه، و«آخر» آخرُ ما أضاف.
 *
 *   و«آخر» تسبق «هاد» في الفحص: «مسح هاد آخر منتوج» نادرة، وحين تقع
 *   فالمقصودُ آخرُ واحدٍ لا ما هو معروضٌ الآن — الأخصُّ يفوز.
 */
//   و«الجديد» **مُخرَجةٌ عمدًا**: «منتوج جديد» طلبُ إنشاءٍ لا إشارةٌ إلى
//   آخرِ ما أُضيف، والصيغتان لا تُفرَّقان بيقين. ومؤشِّرٌ يخطئ هنا يحذف
//   منتوجًا لم يُقصَد — فالغموضُ يُترَك سؤالًا لا يُحسَم تخمينًا.
const REF_LAST = ['اخر', 'الاخر', 'الاخير', 'اللخر', 'لخر', 'dernier', 'derniere'];
const REF_THIS = ['هاد', 'هادا', 'هادي', 'هدا', 'هده', 'هذا', 'هذه', 'داكشي', 'ce', 'cette'];

function readRef(t: string): 'this' | 'last' | undefined {
  // ── **«آخر منتوج» غيرُ «منتوج آخر»** ──────────────────────────
  //
  //   كلمةٌ واحدةٌ بمعنيَين يفصل بينهما **الترتيبُ وحدَه**: «آخِر» قبل الاسم
  //   إشارةٌ إلى واحدٍ بعينه، و«آخَر» بعده تعني «واحدًا غيرَه».
  //
  //   وقِيس على جملةٍ من صاحب المشروع: «بغيت نزيد **منتوج آخر**» ⇒ إشارةٌ
  //   `last` — فيُفهَم أنّه يريد آخرَ منتوجٍ أضافه، وهو يريد **جديدًا**.
  //   والضررُ ليس سؤالًا زائدًا: مع سياقٍ تُحَلّ الإشارةُ إلى منتوجٍ قائم،
  //   فيُساق مَن أراد إضافةً إلى تعديل ما عنده.
  //   والفارقُ **التعريفُ** لا الموضعُ وحدَه، وثلاثُ صيغٍ لا اثنتان:
  //     «**آخر** منتوج»    — مجرّدةٌ **قبل** الاسم  ⇒ إشارةٌ (last)
  //     «المنتوج **الأخير**» — معرّفةٌ            ⇒ إشارةٌ (last)
  //     «منتوج **آخر**»     — مجرّدةٌ **بعد** الاسم ⇒ «واحدٌ غيرُه» ⇒ لا إشارة
  //   فالمجرّدةُ وحدَها هي الملتبِسة، وموضعُها يحسمها.
  const last = REF_LAST.find(w => hitsWord(w, t));
  if (last) {
    // والمجرّدةُ **صفةٌ تتبع موصوفَها**: «منتوج آخر» تُنهي العبارة، و«آخر
    // منتوج» يليها اسمُها. فالفاصلُ ما **بعدَها** لا ما قبلَها — قِيس أوّلًا
    // بموضعها من هدف الفعل فسقطت «بدّل ثمن آخر منتوج»: «آخر» تصف «منتوج»
    // والهدفُ المقروءُ «ثمن»، فقارنتُ بغير ما تصف.
    const bare = last === 'اخر';
    const after = t.slice(t.indexOf(last) + last.length).trim();
    if (!(bare && after === '')) return 'last';
  }
  if (REF_THIS.some(w => hitsWord(w, t))) return 'this';
  return undefined;
}

/** نصٌّ دارجيٌّ لما سيحدث — تعرضه الواجهةُ قبل التنفيذ ليؤكّد الإنسان. */
export function describeAction(a: ActionRead): string {
  const V: Record<ActionVerb, string> = {
    view: 'نوريك', create: 'نزيد', update: 'نبدّل',
    delete: 'نحيّد', share: 'نشارك', send: 'نصيفط',
  };
  const O: Record<ActionObject, string> = {
    phone: 'رقم الهاتف', language: 'اللغة', password: 'كلمة السرّ', account: 'الحساب',
    workspace: 'المحلّ', shop_name: 'اسم المحلّ', shop_hours: 'وقت الخدمة',
    address: 'العنوان', delivery: 'التوصيل', settings: 'الإعدادات',
    product: 'المنتوج', price: 'الثمن', stock: 'المخزون', photo: 'الصورة',
    content: 'الوصف والهاشتاگ',
    channel: 'القنوات المربوطة',
    orders: 'الطلبات', shipment: 'الشحنة', customers: 'الزبناء', coupon: 'الكوبون', wallet: 'المحفظة',
  };
  return `${V[a.verb]} ${O[a.object]}`;
}
