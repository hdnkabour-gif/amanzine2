// ============================================================
// التطبيع — **مصدرٌ واحد**. كان اثني عشر.
//
//   ── ما كشفه القياس ──
//   ظننتُها اثنتي عشرةَ نسخةً متطابقة. وهي ليست كذلك: **خمسةُ مستوياتٍ**
//   مختلفة، أضعفُها في `needEngine` و`humanIntent` — أي في الطريق الموصول
//   بالشاشة. اختبارٌ تفاضليٌّ على ٧٤ حالة (نفسُ الجملة بصيغٍ لا يفرّق بينها
//   إنسان) أعطى **١١ اختلافًا**:
//
//       عندييي محل ديال الخضرة   ⇒ create_store  صارت create_service
//       عندي مشڭل ف الضو          ⇒ find_pro      صارت create_service
//       شحااال الثمن              ⇒ find_pro      صارت unknown
//
//   والثانيةُ أخطرُها: صاحبُ عطلٍ كهربائيٍّ يُقرأ **عارضَ خدمة**. وسببُها
//   حرفٌ واحد: «ڭ» بدل «ك» — وهي لوحةُ مفاتيحَ مغربيّةٌ شائعة. وتكرارُ
//   الحروف («عندييي» · «شحااال») طريقةُ كتابةٍ يوميّة، لا خطأً نادرًا.
//
//   و`understand()` لم يتغيّر في أيٍّ من الـ٧٤ — لأنّه وحدَه يستعمل المطبِّعَ
//   الكامل. فالعطبُ ليس في اللغة بل في **أيِّ نسخةٍ صادفتها الجملة**.
//
//   ── لماذا طبقاتٌ لا دالّةٌ واحدة ──
//   الفرقُ الوحيدُ المشروعُ بين المواضع هو **حذفُ غير العربيّ**: فهرسُ
//   المفاهيم يريد عربيّةً صافية، و`needEngine` يتعامل مع الفرنسيّة وأسماء
//   المدن فيموت لو حُذف غيرُ العربيّ. فالطبقاتُ تحفظ هذا الفرق وتُلغي ما
//   عداه — بدل أن يكون كلُّ ملفٍّ حرًّا في اختراع تطبيعه.
// ============================================================

/** تشكيلٌ وتطويل. `ٰ` و`ـ` كانتا تفوتان النسخَ الضعيفة. */
const AR_DIAC = /[ً-ْٰـ]/g;

export function stripDiacritics(s: string): string {
  return (s || '').replace(AR_DIAC, '');
}

/**
 * توحيدُ الحروف: صيغُ الهمزة · ة→ه · ى→ي · ؤ→و · ئ→ي · والحروفُ الفارسيّة
 * التي تكتب بها لوحاتُ المفاتيح المغربيّة (گ ڭ ⇒ ك · ڤ ⇒ ف · پ ⇒ ب).
 */
export function unifyLetters(s: string): string {
  return (s || '')
    .replace(/[أإآٱ]/g, 'ا').replace(/ة/g, 'ه').replace(/ى/g, 'ي')
    .replace(/ؤ/g, 'و').replace(/ئ/g, 'ي')
    .replace(/[گﮒ]/g, 'ك').replace(/ڭ/g, 'ك').replace(/ڤ/g, 'ف').replace(/پ/g, 'ب');
}

/**
 * «حلااااق» ⇒ «حلاق». ثلاثةٌ فأكثر — فلا تُمَسّ «مدّ» العربيّة الشرعيّة.
 *
 *   ── **والأرقامُ تُستثنى، وهذا عطبٌ في مالِ الناس** ──
 *   كانت القاعدةُ تشمل كلَّ محرف، فابتلعت الأصفار:
 *
 *       «بغيت سباط ب **٢٠٠٠** درهم»   ⇒ ميزانيّة **٢٠ درهمًا**
 *       «متفوتش **١٠٠٠٠** درهم»       ⇒ ميزانيّة **١٠ دراهم**
 *
 *   فمن قال ميزانيّتَه عشرةَ آلافٍ يُعرَض عليه ما ثمنُه عشرة. و«١٥٠٠» تنجو
 *   لأنّ أصفارَها اثنان — فالعطبُ يظهر عند الآلاف وحدَها، وهي المبالغُ التي
 *   يشتري بها المغربيُّ دراجةً أو هاتفًا أو أثاثًا.
 *
 *   والقاعدةُ وُلدت لمدِّ الحروف («حلااااق»)، ولا أحدَ يمدّ رقمًا للتعبير.
 */
export function collapseRepeats(s: string): string {
  return (s || '').replace(/([^\d٠-٩])\1{2,}/g, '$1');
}

const squash = (s: string) => s.replace(/\s+/g, ' ').trim();

/**
 * **علاماتُ الترقيم فاصلٌ لا حرف.**
 *
 *   قِيس: من ١٥١٦ مصطلحًا في الفهرس **١١٠٥** لا تُطابَق في «عندي مشكل،سباك»
 *   وتُطابَق كلُّها في «الطوموبيل خربانة.ميكانيك». والفرقُ ليس في المطابِق:
 *
 *     `normArabic` تُفرّغ كلَّ ما هو **خارج** نطاق العربيّة إلى مسافة. و«،»
 *     و«؟» و«؛» **داخل** نطاق العربيّة في يونيكود (U+060C · U+061F · U+061B)
 *     — فتنجو وتلتصق بالكلمة كأنّها حرفٌ منها. والنقطةُ لاتينيّةٌ فتُفرَّغ.
 *
 *   والمغاربةُ يكتبون «بغيت سباك،كهربائي» بلا مسافة. فكلمةٌ بعد فاصلةٍ
 *   عربيّةٍ كانت لا تُقرأ أبدًا، وبعد نقطةٍ لاتينيّةٍ تُقرأ — سلوكان لرمزَين
 *   يفعلان الشيءَ نفسَه.
 *
 *   وموضعُ الإصلاح **هنا** لا في `hitsArabic`: المطابِقُ واحدٌ من خمسةِ
 *   مستهلكين (الفهرس · الـArabizi · المطابقةُ بالكلمات · المدن · `normLoose`
 *   الذي يقرؤه `needEngine` والاتّجاهُ والحقائق). إصلاحُه وحدَه يُبقي الأربعةَ
 *   الأخرى تقرأ «مشكل،سباك» كلمةً واحدة.
 */
const AR_PUNCT = /[،؍؛؞؟٪٫٬٭۔]/g;
const spacePunct = (s: string) => (s || '').replace(AR_PUNCT, ' ');

/**
 * **التطبيعُ الكامل مع حذف غير العربيّ.** لفهرس المفاهيم: يريد عربيّةً
 * صافيةً فلا تُصادَف مصطلحاتٌ داخل رموزٍ أو لاتينيّة.
 */
export function normArabic(input: string): string {
  let s = collapseRepeats(unifyLetters(stripDiacritics((input || '').toLowerCase())));
  s = spacePunct(s).replace(/[^؀-ۿ0-9\s]/g, ' ');
  return squash(s);
}

/**
 * **التطبيعُ الكامل مع إبقاء غير العربيّ.** لمن يقرأ نصًّا مختلطًا:
 * `needEngine` و`humanIntent` يتعاملان مع الفرنسيّة وأسماء المدن، فحذفُ
 * اللاتينيّة يعميهما. وهذا هو الفرقُ **الوحيدُ المشروع** بين المطبِّعَين.
 */
export function normLoose(input: string): string {
  return squash(spacePunct(collapseRepeats(unifyLetters(stripDiacritics((input || '').toLowerCase())))));
}

/**
 * **حدُّ الكلمة العربيّة** — يطابق كلمةً كاملةً لا جزءًا من كلمة.
 *
 *   كان يعيش نسخةً واحدةً في `merchandise.ts`، فبقيت الطبقاتُ الأخرى تُطابق
 *   بالاحتواء. وثمنُ ذلك مقيسٌ لا مُتخيَّل: «**الما** كيهرب» نمطُ تسرّبِ ماء،
 *   و«**الما**كلة» طعام، و«**الما**كن» آلة. فمن قال «عندي محل ديال الماكلة
 *   كندير طواجن» صار **سبّاكًا** بثقة ٠٫٦٢، ومن قال «الماكن ديال الخياطة
 *   خربات» صار سبّاكًا كذلك. أربعةُ أحرفٍ تبتلع بابَين.
 *
 *   وموضعُه هنا لا هناك: المطبِّعُ واحدٌ للمشروع كلِّه، وحدُّ الكلمة جزءٌ من
 *   المطابقة لا من البضاعة. ونسختان تنحرفان بصمت.
 *
 *   ── والسوابقُ **تتراكب** ──
 *   كانت النسخةُ الأولى تسمح بسابقةٍ واحدةٍ `(ال|ل|ب|ف|و)?`. وكشف سبرٌ أنّ
 *   ذلك يُسقط أشيعَ ما يقوله المغربيّ: «مشكل **فال**طوموبيل» و«**وال**ثلاجة
 *   واقفة» — سابقتان لا واحدة (ف+ال · و+ال)، و«**لل**طوموبيل» إدغامٌ ثالث.
 *   فكان الحدُّ يمنع الابتلاعَ ويُحدِث صممًا مكانَه، وهو الخطأُ المعاكس.
 *
 *   والتوسيعُ لا يُعيد الابتلاع: «الماكلة» تبقى غيرَ مطابقةٍ لـ«ما» لأنّ
 *   الشرطَ الثاني — **نهايةُ الكلمة** — قائمٌ كما هو، و«كلة» تليها.
 *
 * @param term  الكلمةُ المطلوبة — **مطبَّعةٌ سلفًا** بنفس مطبِّع النصّ.
 * @param text  النصُّ المطبَّع.
 */
export function hitsWord(term: string, text: string): boolean {
  return matchWord(term, text) !== null;
}

/**
 * **نفسُ الحدّ، ومعه الموضعُ والطول** — لِمن يحتاج أن يستهلك المقطع.
 *
 *   ── لماذا وُلدت ──
 *   `resolveConcepts` تُطابق بـ`src.indexOf(term)` — **احتواءً بلا حدّ**،
 *   بينما `hitsWord` مبنيّةٌ لهذا منذ زمنٍ وتُستعمَل في مواضعَ أخرى. وثمنُ
 *   ذلك مقيسٌ: «عندي مشكل فال**لابطوب** ديالي» ⇒ **بائعُ أقمشة**، لأنّ
 *   «الطوب» (القماش) تقع داخل «اللابطوب».
 *
 *   ولم تنتقل تلك الدالّةُ إلى `hitsWord` لسببٍ حقيقيّ: هي تحتاج **موضعَ
 *   الإصابة وطولَها** لتمنع التداخل (`taken`)، و`hitsWord` تُرجع نعم/لا.
 *   فبدل أن يُنسَخ الحدُّ هناك بصيغةٍ ثانيةٍ تنحرف بصمت، يُوسَّع هنا **مرّةً
 *   واحدة** وتُبنى `hitsWord` عليه. قاعدةُ الحدّ تبقى في سطرٍ واحد.
 *
 *   والطولُ **يشمل السابقة**: «فالطوموبيل» تُستهلَك كاملةً، وإلّا بقيت «فال»
 *   حرّةً فطابقها مصطلحٌ آخر.
 *
 * @returns موضعُ الإصابة وطولُها بالسوابق، أو `null` إن لم تقع.
 */
export function matchWord(term: string, text: string): { at: number; len: number } | null {
  let esc = term.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  // ── **والتاءُ المربوطةُ تُكتَب بثلاثة أحرف** ─────────────────────
  //
  //   رآه صاحبُ المشروع على شاشته: كتب «بغيت **كسوى** لبنتي» فأجابه التطبيقُ
  //   «كتقلّب على شي حدّ» وعرض عليه **«الماء / السباكة»** و«الكهرباء / الضو».
  //   أبٌ يشتري ثوبًا لابنته سيق إلى سبّاك — **بحرفٍ واحد**: المطبِّعُ يحوّل
  //   `ة⇒ه` و`ى⇒ي`، فصارت «كسوة»⇒«كسوه» و«كسوى»⇒«كسوي». كلمتان في نظر
  //   المحرّك، واحدةٌ في نظر من كتبها.
  //
  //   ── ولماذا هنا لا في المطبِّع ──
  //   جرّبتُ طيَّ «ى» الأخيرة إلى «ه» في `unifyLetters`، **فأسقط حارسٌ قائمٌ
  //   ذلك فورًا**: «عند**ى**» صارت «عنده» بدل «عندي» — فانقلب «عندي محل ديال
  //   الخضرة» من `create_store` إلى `unknown`. والقاعدةُ العامّةُ **تُخمِّن**،
  //   لأنّ «ى» الأخيرةَ تاءٌ مربوطةٌ حينًا وياءٌ حينًا.
  //
  //   والحلُّ ألّا يُخمَّن: **المصطلحُ** هو من يعلن قبولَه. فما انتهى بـ«ه»
  //   في الفهرس يقبل «ي» مكانَها — وهي حالةُ التاء المربوطة وحدَها. وكلامُ
  //   الإنسان لا يُمَسّ، فـ«عندي» تبقى «عندي».
  if (/ه$/.test(term)) esc = esc.replace(/ه$/, '[هي]');
  // «و» تسبق كلَّ شيء، ثمّ حرفُ جرٍّ ملتصقٌ مع «ال» أو بدونها، ثمّ الكلمة.
  const m = new RegExp(`(^|\\s)((?:و)?(?:لل|بال|فال|كال|ال|ل|ب|ف|ك)?${esc})(\\s|$)`).exec(text);
  return m ? { at: m.index + m[1].length, len: m[2].length } : null;
}

/** اللاتينيّة: حروفٌ صغيرة · إزالةُ النبرات (é⇒e) · طيُّ التكرار · رموز. */
export function normLatin(input: string): string {
  let s = (input || '').toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '');
  s = collapseRepeats(s).replace(/[^a-z0-9\s]/g, ' ');
  return squash(s);
}
