# AMANZINE — خارطة الإصلاح المرتّبة (بعد مراجعة ٢١ منطقة)

> المبدأ الحاكم (خلاصة المراجعة): **٧٠٪ من الحلم مبنيٌّ أصلًا — الإصلاح = «وحِّد + رقِّ + اربط»، لا بناءٌ من الصفر.**
> الأولويّات: **P1** يمنع الفهم/يكسر الرحلة · **P2** يمنع البدء/الازدواج · **P3** يُضعف الثقة · **P4** تجميل.

---

## P1 — القلب: باب المحتاج + الفهم الذي لا يتوقّف
| # | الإصلاح | من الاحتكاكات | نوعه |
|---|---|---|---|
| **1** | **توحيد العقل:** محرّكٌ واحدٌ للنيّة `understandHybrid` بدل عقلين (`parseNeed` مقابل `understand/readHuman`). كلّ الصفحات تناديه. | AS3 | توحيد |
| **2** | **قتل «ما فهمناش» نهائيًّا:** في `/assistant` (و`/home`) عند `unknown` ⇒ **السؤال التالي** لا الاعتذار: «فهمت أنّك محتاج… مع من؟ خدمة/منتوج/طلب؟». | AS2 · R3 | ترقية |
| **3** | **باب المحتاج الصحيح:** Hero «آش واقع؟» يفتح **الحوار الموجَّه** (نمط `/assistant` الموجود) بدل رمي المحتاج إلى `/market` فارغ؛ ويعرض **أثر الاستدلال** (تسرّب→سبّاك→مدينة) داخل الحوار — النمط موجودٌ في `/knowledge-studio`. | **M1** · KS1 · AS1 | ربط |
| **4** | **الوجهة تحترم الفهم:** بدل «انشر إعلانك»، المحتاج يُوجَّه لـ**مختصٍّ قريب / حجز خدمة** (`/bookings` هو الجانب الأصدق). | M1 · BK1 | ربط |
| ~~5~~ | ✅ **اللاتينيّة (Arabizi)** — *منجز* (`deArabizi`) · ✅ **إصلاح اختراع المهنة** — *منجز*. | R2 · H1 | مُنجَز |

## P2 — إيقاف الازدواج: المحتاج ≠ التاجر
| # | الإصلاح | من الاحتكاكات | نوعه |
|---|---|---|---|
| 6 | **فصل البداية لا التطبيق:** أوّل شاشةٍ للمحتاج نظيفةٌ من ضجيج التاجر؛ الواجهة **تتشكّل حسب النيّة** (بيع→تاجر، مشكل→محتاج). النمط مُثبَتٌ في `/publish` (بائع) + `/assistant` (محتاج). | LP1·LP4·LP5·H6·PR1·DASH1 | ترقية |
| 7 | **قائمةٌ تكبر مع المستخدم (Progressive Disclosure):** يوم ١ = الرئيسيّة·الطلبات·الرسائل·الحساب؛ الباقي (النشر الموحّد·الاستوديو·الكوبونات…) يظهر بعد أوّل عمل. | H4·H7·BAR1 | ترقية |
| 8 | **إزالة الكلمات الممنوعة من الواجهة:** «المساعد **الذكيّ**»·«**منظومة تجارة**»·«**الذكاء الاصطناعيّ**»·«**السوق الموحّد**» ⇒ لغة بشر. | LP5·GUIDE1·AS4·BAR1 | تنظيف |
| 9 | **الجولة التعريفيّة للمحتاج:** بدل «لوحة أداء متجرك»، تُعلّم «كيف تقول حاجتك». | TOUR1 | ترقية |

## P3 — تقوية الثقة
| # | الإصلاح | من الاحتكاكات |
|---|---|---|
| 10 | إزالة «AI COMMERCE OS» + شارات التاجر عن صفحات المحتاج/الدخول. | L1·LP3 |
| 11 | لا تصنيفٌ متعجّل («فهمت باغي تشري» قبل إنهاء الجملة). | H2 |
| 12 | «MA» (رمز الدولة) الملتصق بالعربيّة + «الثقة x69» الغامض. | M2·PR2 |

## P4 — تجميل
| # | الإصلاح | من الاحتكاكات |
|---|---|---|
| 13 | أسعار «0 د.م» وصورٌ فارغة في السوق/المتجر · «عجلة الحظّ» · التحيّة. | M3·ST1·ST2·H3 |
| 14 | توحيد مثال الشرح «كيهرب» ⇒ «كيقطر/كيسيل». | KS2 |
| 15 | «محرّك سيناريوهات» (jargon) ⇒ لغة بشر. | PUB2 |

---

## الطبقة الذكيّة (بعد P1) — جاهزةٌ للربط
البنية موجودة (`/connections` + `aiChat` + `jsonMode`). الخطوة: نقطة **`POST /api/ai/understand`**
تُعيد استعمال `aiChat()` **ببرومبت فهمٍ منفصلٍ** (JSON نيّة، لا برومبت البائع «Moroccan Seller» الحاليّ).
الـstub في `understanding.ts` يصير نداءً واحدًا. **يتطلّب قرار المزوّد/التكلفة (قرار المالك).**

## أصغر مسارٍ يثبت القيمة (موصى به للبدء)
**P1-1 + P1-2** (توحيد العقل + قتل «ما فهمناش») = **تغييرٌ منطقيٌّ خالص، بلا مسّ تصميم**، قابلٌ
للاختبار بـbrain-tests، ويجعل `/assistant` (الحوار الموجود) يتوقّف عن الاعتذار فورًا. ثمّ P1-3/4 (الباب).

## معيار النجاح (المالك)
> هل يعرف مستخدمٌ لأوّل مرّة، خلال **٣٠ ثانية وبلا قراءة شرح**، ماذا يفعل — ويشعر أنّ التطبيق **فهمه**؟
