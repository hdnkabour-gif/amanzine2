# إصلاح: تتبّع شحنات Livo الحقيقي (خطوة صغيرة وإضافية)

> لا حذف، لا إعادة هيكلة. 7 ملفات فقط، كلها تعديلات إضافية على كود موجود.
> تم فعلياً: `npm install`، `npx tsc --noEmit` (بدون أخطاء جديدة)، `npm run build` (نجح).

---

## المشكلة الحقيقية اللي لقيتها

`server/services/delivery/providers/livo.provider.js` فيه دالة `getOrderStatus()` كاملة
ومكتوبة مليح — لكنها **ما كتُستدعى من حتى مكان** فالمشروع. يعني: كتخلق شحنة عند Livo،
كتوصل رقم تتبع، وهذا يبان للتاجر... لكن بعد داك، ما كاين حتى طريقة باش تشوف واش الشحنة
"فالطريق" ولا "توصلات" ولا "تلغات" — غير كتبقى `processing` للأبد فالتطبيق ديالك، حتى إلا
تبدلات الحالة عند Livo نفسها.

والأسوأ: كود `server/routes/delivery.js` كان كيحاول يخزن `livo_order_id` (معرّف الشحنة عند
Livo، ضروري باش تقدر تسولها عن حالتها لاحقاً)، لكن:
1. عمود `livo_order_id` ما كانش كاين فجدول `orders` أصلاً.
2. حتى إلا كان كاين، دالة `updateOrder()` فـ `database.js` عندها "لائحة بيضاء" للأعمدة
   المسموح تحديثها، و`livo_order_id` ما كانش فيها.

يعني: المعرّف كان كيتحسب، وكيتبعث لـ`updateOrder`، ثم **كيضيع بصمت** بلا أي خطأ يبان.
هادشي خطير لأنه بدون هاد المعرّف، ما تقدرش أبداً تبني تتبّع حقيقي — حتى لو زدت واجهة جميلة.

## شنو تصلح بالضبط

### `server/migrate.js`
زدت 3 أعمدة إضافية لجدول `orders` (بـ `IF NOT EXISTS` — آمنة 100% على قاعدة بيانات فيها بيانات):
- `livo_order_id` — معرّف الشحنة عند Livo (اللي كان كيضيع).
- `delivery_status` — آخر حالة توصّلات من Livo (نص خام، بلا ترجمة مفبركة — ما عنديش
  توثيق Livo الرسمي لكل القيم الممكنة، فبقيت أمين وما خمنتش).
- `delivery_synced_at` — وقت آخر تحديث.

### `server/database.js`
- زدت الثلاثة حقول فـ `_mapOrder()` (باش يبانو فكل مكان كيرجع فيه الطلب للواجهة).
- زدتهم فلائحة `updateOrder()` البيضاء — هادي هي الإصلاح الأساسي للمشكلة.

### `server/routes/delivery.js`
- صححت السطر اللي كان كيبعث `livo_order_id` (بصيغة خاطئة، snake_case) → دابا `livoOrderId`
  (camelCase) باش يطابق المفتاح الجديد فـ `database.js` ويتخزن فعلاً.
- زدت endpoint جديد: **`POST /api/delivery/track/:orderId`** — كيدير نداء حقيقي لـ
  `livoProvider.getOrderStatus()` (اللي كانت موجودة ومهملة)، كيحدث `delivery_status` و
  `delivery_synced_at`، وكيسجل فالـ logs. تحديث يدوي بالطلب (بضغطة زر) — بلا polling
  تلقائي فالخلفية، باش نبقاو فنفس الفلسفة ديال "خطوة صغيرة حقيقية".

### `src/types.ts`, `src/services/api.ts`, `src/store.tsx`, `src/pages/OrdersPage.tsx`
- زدت الحقول الجديدة فـ `Order` type.
- زدت `deliveryAPI.track()` فخدمة الـ API.
- زدت `trackOrder()` فـ store (نفس أسلوب `shipOrder`/`deliverOrder` بالضبط — صادق فالأخطاء،
  ما كيخبيش الفشل).
- فـ `OrdersPage.tsx`: زر "🔄 تحديث حالة الشحن" كيبان غير إلا كان الطلب عندو `livoOrderId`
  حقيقي (يعني: طلب جديد شُحن عبر Livo API فعلاً، ماشي محاكاة). كيبان جنب رقم التتبع، مع آخر
  حالة معروفة ووقت آخر تحديث.

## واش يخص شي حاجة خرى؟

لا. ماكسرتش:
- مسارات Amana/Jibli/Webhook/محاكاة فـ `delivery.js` — بقات بحالها.
- أي عمود أو endpoint قديم.
- التاجرين اللي عندهم شحنات قديمة (قبل هاد التحديث) — غادي يبان ليهم الزر فقط إلا كان
  `livoOrderId` محفوظ، والشحنات القديمة ماعندهاش هاد المعرف (كان كيضيع) — غادي يبقاو
  بحالهم بلا الزر، بلا أي خطأ.

## للنشر

فقط: `git push` عادي، Railway غادي يخدم `npm run postinstall` → `migrate` بحسب
`nixpacks.toml`/`railway.json` ديالك الحاليين (ما بدلتش فيهم شي حاجة). الأعمدة الجديدة
غادي تتزاد أوتوماتيكياً عند أول تشغيل، بلا ما يمس البيانات الموجودة.

## الخطوة الجاية (اختيارية، ماشي دابا)

إلا كان Livo كيدعم Webhooks حقيقية (خاصنا نتأكدو من التوثيق الرسمي ديالها)، نقدر نبدلو
التحديث اليدوي بـ webhook receiver — تحديث تلقائي بلا ما التاجر يضغط زر. لكن هادي خطوة
لاحقة، ماشي شرط باش هاد الإصلاح يخدم اليوم.

---

## تتمة (نفس الجلسة): وصول الحالة للزبون نفسه

لقيت أن `delivery_status` اللي زدناها كانت كتوصل غير للتاجر (فـ `OrdersPage.tsx`)، والزبون
اللي كيتبع الطلب ديالو بالكود أو الهاتف (`Storefront.tsx` → `TrackingModal`) ما كان
عندو حتى وصول ليها — كيشوف غير الحالة الداخلية الخشنة ديال AMANZINE (قيد التحضير/في
الطريق/وصل...) بلا التفاصيل الحقيقية من Livo.

### زدت (4 ملفات إضافية):
- **`server/routes/orders.js`**: زدت `deliveryStatus` فجواب `GET /api/orders/track/:phone`
  و `GET /api/orders/track-code/:code` — نفس الحقل اللي زدناه فالخطوة السابقة، غير كتوصل
  دابا للـ endpoints العامّة (بلا تسجيل دخول) اللي كيستعملها الزبون.
- **`src/pages/Storefront.tsx`** (`TrackingModal`): سطر صغير 🚚 كيبان تحت المنتجات، فحالتين
  (البحث بالكود والبحث بالهاتف)، غير إلا كان `deliveryStatus` موجود — بلا ما يبدل أي حاجة
  فالتصميم أو التنسيق الموجود.

**تم التحقق:** `node --check` على `orders.js`، و`npx tsc --noEmit` + `npm run build`
كاملين — بلا أي خطأ جديد فالملفات اللي تبدلات.

⚠️ ملاحظة صادقة: هاد الحقل يبقى فارغاً عند الزبون حتى التاجر يضغط "🔄 تحديث حالة الشحن"
مرة وحدة على الأقل (لا يوجد تحديث تلقائي بعد — هذا مقصود، خطوة صغيرة فخطوة صغيرة).

---

## تتمة ثانية (نفس الجلسة): المشكلة الجذرية الحقيقية — "لا توجد شركة توصيل مفعّلة"

هاد واحدة كانت **أخطر بزاف** من الأولى، ولقيتها بعدما بعثتي ليا رسائل الخطأ الحقيقية من التطبيق.

### المشكلة

`src/pages/DeliveryPage.tsx` (صفحة "شركات التوصيل" كاملة — 4 نماذج الإضافة + زر التفعيل/التعطيل
+ زر الحذف) كانت كتكتب **فقط** فـ `settings.delivery.providers` — وهي مجرد JSON مخزّن فجدول
الإعدادات العام (`db.saveSettings`) — **ولم تكن أبداً تكتب فجدول `delivery_providers` الحقيقي**
اللي كيقرا منه `server/routes/delivery.js` عند إنشاء شحنة فعلية (`db.getDeliveryProviders`).

يعني: كنت تزيد Livo، تدخل مفتاح API، تفعّلها، تشوفها "🟢 مفعّلة" فالصفحة — وكل هذا **حقيقي فالواجهة
فقط**. السيرفر، لما يجي يشحن طلب، كيسول جدول `delivery_providers` (فارغ)، فيرجع "لا توجد شركة
توصيل مفعّلة" — رغم أن كل شيء كيبان مضبوط قدامك.

### الإصلاح (4 ملفات، هاد المرة)

- **`server/database.js`**: `upsertDeliveryProvider()` دابا كترجع `id` الصف الحقيقي (كانت ما
  كترجع والو قبل).
- **`server/routes/delivery.js`**: `POST /api/delivery` دابا كيرجع `id` فالجواب.
- **`src/types.ts`**: زدت `apiType?` و `dbId?` لـ `DeliveryProviderConfig` (كان `apiType`
  كيتستعمل فالكود من قبل بلا ما يكون معرّف فالـ type — هادشي كان سبب 3 من 4 أخطاء TypeScript
  القديمة، تصلحو بصفة جانبية 🎉).
- **`src/pages/DeliveryPage.tsx`** — 3 تعديلات:
  1. `saveApi()` (إضافة مزوّد بمفتاح API): دابا كتكتب **أيضاً** فـ `delivery_providers` الحقيقي
     عبر `deliveryAPI.save()`، وكتحتفظ بالمعرّف الحقيقي (`dbId`) فالإعدادات.
  2. **زر التفعيل/التعطيل**: دابا كيحدّث `enabled` فقاعدة البيانات الحقيقية أيضاً (كان الزر
     التفعيل الأصلي كيبدّل غير فالواجهة — وهاد بالضبط الحقل اللي كيفلتر عليه السيرفر).
  3. **الحذف**: دابا كيحذف الصف الحقيقي أيضاً إلا كان موجود.
  4. **إصلاح تلقائي لمرة واحدة (مهم بزاف)**: عند فتح صفحة التوصيل، أي مزوّد بمفتاح API (كيما
     Livo ديالك الحالي) ماعندوش `dbId` (يعني تزاد قبل هاد الإصلاح) — كيتصلح تلقائياً وبصمت،
     بلا ما تحتاج تحذفو وتزيدو من جديد. **هادشي كيصلح Livo ديالك الحالي مباشرة بمجرد ما تفتح
     صفحة التوصيل بعد النشر.**

### تم التحقق فعلياً
- `node --check` على الملفين الخلفيين ✅
- `npx tsc --noEmit`: **4 أخطاء → 1 خطأ وحيد** غير مرتبط بشي حاجة بدلتها (كان موجود قبل) ✅
- `npm run build`: نجح كاملاً ✅

### واش يخص شي حاجة؟
لا. النماذج التلاتة الأخرى (Simple/WhatsApp/URL-Recipe) بقاو بحالهم بلا تغيير — ما بدلتهمش
فهاد الجولة لتفادي التوسع الزايد، خاصة أن الرابطين (Simple وWhatsApp) ما عندهمش `apiType`
حقيقي أصلاً، وURL-Recipe كيخزن الوصفة بطريقة مختلفة (JSON فداخل `fields`) وخاصو تفكير منفصل
إلا بغيتي تزادو لهاد المزامنة فخطوة جاية.
